diff --git a/include/svx/svdotable.hxx b/include/svx/svdotable.hxx
index 4a25ae970..7edb5c267 100644
--- a/include/svx/svdotable.hxx
+++ b/include/svx/svdotable.hxx
@@ -258,6 +258,10 @@ public:
 
     void LayoutTableHeight(tools::Rectangle& rArea);
 
+    /// Keep the externally reported table size aligned with the completed
+    /// row/column layout without redistributing the live cell dimensions.
+    void NbcSyncLogicRectToTableLayout();
+
     virtual void onEditOutlinerStatusEvent( EditStatus* pEditStatus ) override;
 
     virtual void dumpAsXml(xmlTextWriterPtr pWriter) const override;
diff --git a/oox/inc/drawingml/textcharacterproperties.hxx b/oox/inc/drawingml/textcharacterproperties.hxx
index 40842585d..8a94dd4a8 100644
--- a/oox/inc/drawingml/textcharacterproperties.hxx
+++ b/oox/inc/drawingml/textcharacterproperties.hxx
@@ -28,10 +28,13 @@
 #include <drawingml/fillproperties.hxx>
 #include <drawingml/lineproperties.hxx>
 
+#include <memory>
+
 namespace oox { class PropertySet; }
 
 namespace oox::drawingml {
 
+struct EffectProperties;
 
 struct TextCharacterProperties
 {
@@ -65,6 +68,7 @@ struct TextCharacterProperties
     bool mbHasVisualRunProperties;
 
     std::vector<css::beans::PropertyValue> maTextEffectsProperties;
+    std::shared_ptr<EffectProperties> mpEffectPropertiesPtr;
 
     /** Overwrites all members that are explicitly set in rSourceProps. */
     void                assignUsed( const TextCharacterProperties& rSourceProps );
@@ -84,7 +88,9 @@ struct TextCharacterProperties
                             PropertySet& rPropSet,
                             const ::oox::core::XmlFilterBase& rFilter ) const;
 
-    TextCharacterProperties() : mbHasVisualRunProperties(false) {}
+    EffectProperties& getEffectProperties() const;
+
+    TextCharacterProperties();
 };
 
 
diff --git a/oox/source/drawingml/textcharacterproperties.cxx b/oox/source/drawingml/textcharacterproperties.cxx
index d123f3607..a6555d761 100644
--- a/oox/source/drawingml/textcharacterproperties.cxx
+++ b/oox/source/drawingml/textcharacterproperties.cxx
@@ -34,6 +34,10 @@
 #include <oox/token/properties.hxx>
 #include <oox/token/tokens.hxx>
 
+#include "effectproperties.hxx"
+
+#include <algorithm>
+
 using ::oox::core::XmlFilterBase;
 using namespace ::com::sun::star;
 using namespace ::com::sun::star::uno;
@@ -41,6 +45,17 @@ using namespace ::com::sun::star::beans;
 
 namespace oox::drawingml {
 
+TextCharacterProperties::TextCharacterProperties()
+    : mbHasVisualRunProperties(false)
+    , mpEffectPropertiesPtr(std::make_shared<EffectProperties>())
+{
+}
+
+EffectProperties& TextCharacterProperties::getEffectProperties() const
+{
+    return *mpEffectPropertiesPtr;
+}
+
 void TextCharacterProperties::assignUsed( const TextCharacterProperties& rSourceProps )
 {
     // overwrite all properties existing in rSourceProps
@@ -69,6 +84,7 @@ void TextCharacterProperties::assignUsed( const TextCharacterProperties& rSource
     assignIfUsed( moTextOutlineProperties, rSourceProps.moTextOutlineProperties);
 
     maTextEffectsProperties = rSourceProps.maTextEffectsProperties;
+    mpEffectPropertiesPtr->assignUsed(*rSourceProps.mpEffectPropertiesPtr);
     maFillProperties.assignUsed( rSourceProps.maFillProperties );
 }
 
@@ -182,6 +198,12 @@ void TextCharacterProperties::pushToPropMap( PropertyMap& rPropMap, const XmlFil
     rPropMap.setProperty( PROP_CharUnderline, GetFontUnderline( moUnderline.value_or( XML_none ) ));
     rPropMap.setProperty( PROP_CharStrikeout, GetFontStrikeout( moStrikeout.value_or( XML_noStrike ) ));
     rPropMap.setProperty( PROP_CharCaseMap, GetCaseMap( moCaseMap.value_or( XML_none ) ));
+    if (std::any_of(mpEffectPropertiesPtr->m_Effects.begin(),
+                    mpEffectPropertiesPtr->m_Effects.end(),
+                    [](const auto& pEffect) {
+                        return pEffect && pEffect->msName == "outerShdw";
+                    }))
+        rPropMap.setProperty(PROP_CharShadowed, true);
 
     if( moBaseline.has_value() ) {
         rPropMap.setProperty( PROP_CharEscapement, sal_Int16(moBaseline.value_or( 0 ) / 1000));
diff --git a/oox/source/drawingml/textcharacterpropertiescontext.cxx b/oox/source/drawingml/textcharacterpropertiescontext.cxx
index fb4390e4c..d211bd67c 100644
--- a/oox/source/drawingml/textcharacterpropertiescontext.cxx
+++ b/oox/source/drawingml/textcharacterpropertiescontext.cxx
@@ -21,6 +21,7 @@
 
 #include <oox/helper/attributelist.hxx>
 #include <drawingml/colorchoicecontext.hxx>
+#include <drawingml/effectpropertiescontext.hxx>
 #include <drawingml/linepropertiescontext.hxx>
 #include <drawingml/misccontexts.hxx>
 #include <drawingml/textcharacterproperties.hxx>
@@ -128,7 +129,8 @@ ContextHandlerRef TextCharacterPropertiesContext::onCreateContext( sal_Int32 aEl
         // EG_EffectProperties
         case A_TOKEN( effectDag ):  // CT_EffectContainer 5.1.10.25
         case A_TOKEN( effectLst ):  // CT_EffectList 5.1.10.26
-        break;
+            return new EffectPropertiesContext(
+                *this, mrTextCharacterProperties.getEffectProperties());
         case A_TOKEN( highlight ):  // CT_Color
             return new ColorContext(*this, mrTextCharacterProperties.maHighlightColor);
         case W_TOKEN( highlight ):
diff --git a/oox/source/export/drawingml.cxx b/oox/source/export/drawingml.cxx
index a3e5356c0..5db27f8b5 100644
--- a/oox/source/export/drawingml.cxx
+++ b/oox/source/export/drawingml.cxx
@@ -2726,6 +2726,21 @@ void DrawingML::WriteRunProperties( const Reference< XPropertySet >& rRun, bool
         }
     }
 
+    bool bCharShadowed = false;
+    if (GetProperty(rXPropSet, u"CharShadowed"_ustr))
+        mAny >>= bCharShadowed;
+    if (GetDocumentType() == DOCUMENT_PPTX && bCharShadowed)
+    {
+        mpFS->startElementNS(XML_a, XML_effectLst);
+        mpFS->startElementNS(XML_a, XML_outerShdw, XML_blurRad, "0", XML_dist, "12700",
+                             XML_dir, "2700000", XML_algn, "tl", XML_rotWithShape, "0");
+        mpFS->startElementNS(XML_a, XML_srgbClr, XML_val, "000000");
+        mpFS->singleElementNS(XML_a, XML_alpha, XML_val, "50000");
+        mpFS->endElementNS(XML_a, XML_srgbClr);
+        mpFS->endElementNS(XML_a, XML_outerShdw);
+        mpFS->endElementNS(XML_a, XML_effectLst);
+    }
+
     // tdf#128096, exporting XML_highlight to docx already works fine,
     // so make sure this code is only run when exporting to pptx, just in case
     if (GetDocumentType() == DOCUMENT_PPTX)
diff --git a/sd/inc/drawdoc.hxx b/sd/inc/drawdoc.hxx
index dd6b81647..5f0d7c55c 100644
--- a/sd/inc/drawdoc.hxx
+++ b/sd/inc/drawdoc.hxx
@@ -535,7 +535,8 @@ public:
         AutoLayout eNotesLayout,
         bool bIsPageBack,
         bool bIsPageObj,
-        const sal_Int32 nInsertPosition);
+        const sal_Int32 nInsertPosition,
+        bool bPreserveMaster = false);
 
     /** This method acts as a simplified front end for the more complex
         <member>DuplicatePage()</member> method.
diff --git a/sd/inc/sdpage.hxx b/sd/inc/sdpage.hxx
index ef127ea60..6e5eee689 100644
--- a/sd/inc/sdpage.hxx
+++ b/sd/inc/sdpage.hxx
@@ -187,7 +187,8 @@ public:
     /** inserts the given SdrObject into the presentation object list */
     void            InsertPresObj(SdrObject* pObj, PresObjKind eKind );
 
-    void            SetAutoLayout(AutoLayout eLayout, bool bInit=false, bool bCreate=false);
+    void            SetAutoLayout(AutoLayout eLayout, bool bInit=false, bool bCreate=false,
+                                  bool bPreserveMaster=false);
     AutoLayout      GetAutoLayout() const { return meAutoLayout; }
     void            CreateTitleAndLayout(bool bInit=false, bool bCreate=false);
     SdrObject*      InsertAutoLayoutShape(SdrObject* pObj, PresObjKind eObjKind, bool bVertical, const ::tools::Rectangle& rRect, bool bInit);
diff --git a/sd/source/core/drawdoc2.cxx b/sd/source/core/drawdoc2.cxx
index 8f7c9f332..a7f20cc4c 100644
--- a/sd/source/core/drawdoc2.cxx
+++ b/sd/source/core/drawdoc2.cxx
@@ -1136,7 +1136,8 @@ sal_uInt16 SdDrawDocument::CreatePage (
     AutoLayout eNotesLayout,
     bool bIsPageBack,
     bool bIsPageObj,
-    const sal_Int32 nInsertPosition)
+    const sal_Int32 nInsertPosition,
+    bool bPreserveMaster)
 {
     SdPage* pPreviousStandardPage;
     SdPage* pPreviousNotesPage;
@@ -1176,7 +1177,8 @@ sal_uInt16 SdDrawDocument::CreatePage (
 
     // User layout of current standard page
     pStandardPage->SetLayoutName( pPreviousStandardPage->GetLayoutName() );
-    pStandardPage->SetAutoLayout(eStandardLayout, true);
+    pStandardPage->SetAutoLayout(eStandardLayout, true, false,
+                                 bPreserveMaster);
     pStandardPage->setHeaderFooterSettings( pPreviousStandardPage->getHeaderFooterSettings() );
 
     // transition settings of current page
diff --git a/sd/source/core/sdpage.cxx b/sd/source/core/sdpage.cxx
index 10b4f34b7..7410c81bd 100644
--- a/sd/source/core/sdpage.cxx
+++ b/sd/source/core/sdpage.cxx
@@ -1608,7 +1608,7 @@ static void findAutoLayoutShapesImpl( SdPage& rPage, const LayoutDescriptor& rDe
     }
 }
 
-void SdPage::SetAutoLayout(AutoLayout eLayout, bool bInit, bool bCreate )
+void SdPage::SetAutoLayout(AutoLayout eLayout, bool bInit, bool bCreate, bool bPreserveMaster)
 {
     sd::ScopeLockGuard aGuard( maLockAutoLayoutArrangement );
 
@@ -1619,8 +1619,14 @@ void SdPage::SetAutoLayout(AutoLayout eLayout, bool bInit, bool bCreate )
 
     meAutoLayout = eLayout;
 
-    // if needed, creates and initialises the presentation shapes on this slides master page
-    CreateTitleAndLayout(bInit, bCreate);
+    // Ordinary Impress layout changes keep the historical behavior of
+    // initialising master placeholders and its default background. A typed
+    // document transform can explicitly preserve an imported PPTX master and
+    // arrange only the slide objects; CalcAutoLayoutRectangles() already falls
+    // back to the page's default title/body rectangles when the master has no
+    // matching placeholders.
+    if (!bPreserveMaster)
+        CreateTitleAndLayout(bInit, bCreate);
 
     if((meAutoLayout == AUTOLAYOUT_NONE && maPresentationShapeList.isEmpty()) || mbMaster)
     {
diff --git a/sd/source/filter/eppt/pptx-epptooxml.cxx b/sd/source/filter/eppt/pptx-epptooxml.cxx
index 96f135ecf..c49efe395 100644
--- a/sd/source/filter/eppt/pptx-epptooxml.cxx
+++ b/sd/source/filter/eppt/pptx-epptooxml.cxx
@@ -1360,7 +1360,14 @@ void PowerPointExport::ImplWriteSlide(sal_uInt32 nPageNum, sal_uInt32 nMasterNum
 
     pFS->startElementNS(XML_p, XML_sld, PNMSS, XML_show, pShow, XML_showMasterSp, pShowMasterShape);
 
-    pFS->startElementNS(XML_p, XML_cSld);
+    OUString aSlideName;
+    Reference<XNamed> xNamed(mXDrawPage, UNO_QUERY);
+    if (xNamed.is())
+        aSlideName = xNamed->getName();
+    if (aSlideName.isEmpty())
+        pFS->startElementNS(XML_p, XML_cSld);
+    else
+        pFS->startElementNS(XML_p, XML_cSld, XML_name, aSlideName);
 
     // background
     if (bHasBackground)
diff --git a/sd/source/ui/inc/unmodpg.hxx b/sd/source/ui/inc/unmodpg.hxx
index 02920c27a..8beecf217 100644
--- a/sd/source/ui/inc/unmodpg.hxx
+++ b/sd/source/ui/inc/unmodpg.hxx
@@ -40,6 +40,7 @@ class ModifyPageUndoAction final : public SdUndoAction
     bool        mbNewBckgrndVisible;
     bool        mbOldBckgrndObjsVisible;
     bool        mbNewBckgrndObjsVisible;
+    bool        mbPreserveMaster;
 
 public:
     ModifyPageUndoAction(
@@ -48,7 +49,8 @@ public:
         const OUString&         aTheNewName,
         AutoLayout              eTheNewAutoLayout,
         bool                    bTheNewBckgrndVisible,
-        bool                    bTheNewBckgrndObjsVisible);
+        bool                    bTheNewBckgrndObjsVisible,
+        bool                    bPreserveMaster = false);
 
     virtual ~ModifyPageUndoAction() override;
     virtual void Undo() override;
diff --git a/sd/source/ui/unoidl/unopage.cxx b/sd/source/ui/unoidl/unopage.cxx
index 1462e08e3..1578ecd97 100644
--- a/sd/source/ui/unoidl/unopage.cxx
+++ b/sd/source/ui/unoidl/unopage.cxx
@@ -48,6 +48,7 @@
 #include <unopage.hxx>
 #include <svl/itemset.hxx>
 #include <svx/svdmodel.hxx>
+#include <svx/svdundo.hxx>
 #include <sdresid.hxx>
 #include <strings.hrc>
 #include <sdpage.hxx>
@@ -2440,7 +2441,22 @@ sal_Bool SAL_CALL SdDrawPage::hasElements()
 // XShapes
 void SAL_CALL SdDrawPage::add( const Reference< drawing::XShape >& xShape )
 {
+    SdrObject* pBefore = SdrObject::getSdrObjectFromXShape(xShape);
+    const bool bAlreadyOnThisPage
+        = pBefore && pBefore->getParentSdrObjListFromSdrObject() == GetPage();
     SdGenericDrawPage::add( xShape );
+
+    // XShapes::add is the common public creation boundary used by browser
+    // clients. The generic UNO implementation inserts a fully configured
+    // object but does not register native Undo. Record that insertion here,
+    // once and only for a genuinely new object on an ordinary Impress slide,
+    // so every UNO-created shape retains its exact service, content and
+    // geometry without a clipboard round trip.
+    SdrObject* pInserted = SdrObject::getSdrObjectFromXShape(xShape);
+    SdDrawDocument* pDocument = GetModel() ? GetModel()->GetDoc() : nullptr;
+    if (!bAlreadyOnThisPage && pDocument && pDocument->IsUndoEnabled() && pInserted
+        && pInserted->getParentSdrObjListFromSdrObject() == GetPage())
+        pDocument->AddUndo(SdrUndoFactory::CreateUndoNewObject(*pInserted));
 }
 
 void SAL_CALL SdDrawPage::remove( const Reference< drawing::XShape >& xShape )
diff --git a/sd/source/ui/view/unmodpg.cxx b/sd/source/ui/view/unmodpg.cxx
index 4d9c52b03..95608e822 100644
--- a/sd/source/ui/view/unmodpg.cxx
+++ b/sd/source/ui/view/unmodpg.cxx
@@ -49,7 +49,8 @@ ModifyPageUndoAction::ModifyPageUndoAction(
     const OUString& aTheNewName,
     AutoLayout  eTheNewAutoLayout,
     bool bTheNewBckgrndVisible,
-    bool bTheNewBckgrndObjsVisible)
+    bool bTheNewBckgrndObjsVisible,
+    bool bPreserveMaster)
 :   SdUndoAction(pTheDoc)
 {
     DBG_ASSERT(pThePage, "Undo without a page???");
@@ -59,6 +60,7 @@ ModifyPageUndoAction::ModifyPageUndoAction(
     meNewAutoLayout         = eTheNewAutoLayout;
     mbNewBckgrndVisible     = bTheNewBckgrndVisible;
     mbNewBckgrndObjsVisible = bTheNewBckgrndObjsVisible;
+    mbPreserveMaster        = bPreserveMaster;
 
     meOldAutoLayout         = mpPage->GetAutoLayout();
 
@@ -99,7 +101,10 @@ void ModifyPageUndoAction::Undo()
                 pView->UnmarkAll();
         });
 
-    mpPage->SetAutoLayout( meOldAutoLayout );
+    // Typed document transforms can change the slide layout without rewriting
+    // an imported PPTX master. Undo must use the same boundary; the nested
+    // object actions in the list restore the slide objects themselves.
+    mpPage->SetAutoLayout(meOldAutoLayout, false, false, mbPreserveMaster);
 
     if (!mpPage->IsMasterPage())
     {
diff --git a/svx/source/inc/cell.hxx b/svx/source/inc/cell.hxx
index b70c683dd..57901b1c4 100644
--- a/svx/source/inc/cell.hxx
+++ b/svx/source/inc/cell.hxx
@@ -95,7 +95,8 @@ public:
 
     SVX_DLLPRIVATE void setMerged();
 
-    SVX_DLLPRIVATE void copyFormatFrom( const CellRef& xSourceCell );
+    SVX_DLLPRIVATE void copyFormatFrom( const CellRef& xSourceCell,
+                                        bool bNotifyModified = true );
 
     // XInterface
     SVX_DLLPRIVATE virtual css::uno::Any SAL_CALL queryInterface( const css::uno::Type& Type ) override;
diff --git a/svx/source/svdraw/svdundo.cxx b/svx/source/svdraw/svdundo.cxx
index 0dcfede6e..d425a53e5 100644
--- a/svx/source/svdraw/svdundo.cxx
+++ b/svx/source/svdraw/svdundo.cxx
@@ -25,6 +25,7 @@
 #include <svx/svdpage.hxx>
 #include <svx/svdlayer.hxx>
 #include <svx/svdmodel.hxx>
+#include <svx/sdr/contact/viewcontact.hxx>
 #include <svx/svdview.hxx>
 #include <svx/xbtmpit.hxx>
 #include <svx/xfillit0.hxx>
@@ -1042,6 +1043,9 @@ void SdrUndoObjSetText::Undo()
     {
         // copy text for Undo, because the original now belongs to SetOutlinerParaObject()
         pTarget->NbcSetOutlinerParaObjectForText(pOldText, pText);
+        if (pText->GetOutlinerParaObject())
+            pText->ReformatText();
+        pTarget->GetViewContact().flushViewObjectContacts(false);
     }
 
     pTarget->SetEmptyPresObj(bEmptyPresObj);
@@ -1075,6 +1079,9 @@ void SdrUndoObjSetText::Redo()
     {
         // copy text for Undo, because the original now belongs to SetOutlinerParaObject()
         pTarget->NbcSetOutlinerParaObjectForText( pNewText, pText );
+        if (pText->GetOutlinerParaObject())
+            pText->ReformatText();
+        pTarget->GetViewContact().flushViewObjectContacts(false);
     }
 
     pTarget->ActionChanged();
diff --git a/svx/source/table/cell.cxx b/svx/source/table/cell.cxx
index 7147bf077..181fd25fa 100644
--- a/svx/source/table/cell.cxx
+++ b/svx/source/table/cell.cxx
@@ -498,7 +498,7 @@ void Cell::setMerged()
 }
 
 
-void Cell::copyFormatFrom( const CellRef& xSourceCell )
+void Cell::copyFormatFrom( const CellRef& xSourceCell, bool bNotifyModified )
 {
     if( !(xSourceCell.is() && mpProperties) )
         return;
@@ -514,7 +514,8 @@ void Cell::copyFormatFrom( const CellRef& xSourceCell )
         SetStyleSheet( nullptr, true );
     }
 
-    notifyModified();
+    if (bNotifyModified)
+        notifyModified();
 }
 
 
@@ -1693,7 +1694,6 @@ void SAL_CALL Cell::setString( const OUString& aString )
     if (getString() == aString)
         return;
 
-    AddUndo();
     SvxUnoTextBase::setString( aString );
     notifyModified();
 }
diff --git a/svx/source/table/svdotable.cxx b/svx/source/table/svdotable.cxx
index 9398a2c98..8048454c3 100644
--- a/svx/source/table/svdotable.cxx
+++ b/svx/source/table/svdotable.cxx
@@ -2357,6 +2357,12 @@ void SdrTableObj::RestoreGeoData(const SdrObjGeoData& rGeo)
 
     SdrTextObj::RestoreGeoData (rGeo);
 
+    // The base geometry restore rebuilds its rectangle using closed Size
+    // semantics. Reapply the table-specific logical rectangle captured by
+    // SaveGeoData so an Undo/Redo round trip preserves the externally
+    // observable half-open UNO width and height exactly.
+    maLogicRect = static_cast<const TableObjectGeoData &>(rGeo).maLogicRect;
+
     if( mpImpl.is() )
     {
         auto aRectangle = getRectangle();
@@ -2384,6 +2390,18 @@ void SdrTableObj::LayoutTableHeight(tools::Rectangle& rArea)
     }
 }
 
+void SdrTableObj::NbcSyncLogicRectToTableLayout()
+{
+    // SvxShape::getSize() reports the half-open size of maLogicRect. The table
+    // layouter already stores its row/column sums as that same half-open
+    // extent, so copying the completed layout rectangle is the exact
+    // conversion. GetWidth()/GetHeight() would add one 1/100 mm here because
+    // those methods deliberately use closed-interval semantics.
+    const tools::Rectangle& rLayoutRect = getRectangle();
+    maLogicRect = rLayoutRect;
+    SetBoundAndSnapRectsDirty();
+}
+
 void SdrTableObj::DistributeColumns( sal_Int32 nFirstColumn, sal_Int32 nLastColumn, const bool bOptimize, const bool bMinimize )
 {
     if( mpImpl.is() && mpImpl->mpLayouter )
diff --git a/svx/source/table/tablecolumn.cxx b/svx/source/table/tablecolumn.cxx
index efa5f4137..2f27fb7b4 100644
--- a/svx/source/table/tablecolumn.cxx
+++ b/svx/source/table/tablecolumn.cxx
@@ -218,6 +218,7 @@ void SAL_CALL TableColumn::setFastPropertyValue( sal_Int32 nHandle, const Any& a
             rModel.AddUndo( std::move(pUndo) );
         }
         mxTableModel->setModified(true);
+        mxTableModel->getSdrTableObj()->NbcSyncLogicRectToTableLayout();
     }
 }
 
diff --git a/svx/source/table/tablemodel.cxx b/svx/source/table/tablemodel.cxx
index 982153976..b614ec81b 100644
--- a/svx/source/table/tablemodel.cxx
+++ b/svx/source/table/tablemodel.cxx
@@ -201,71 +201,83 @@ uno::Reference<css::table::XTable> TableModel::getTable()
 
 void TableModel::UndoInsertRows( sal_Int32 nIndex, sal_Int32 nCount )
 {
-    TableModelNotifyGuard aGuard( this );
+    {
+        TableModelNotifyGuard aGuard( this );
 
-    // remove the rows
-    remove_range<RowVector,RowVector::iterator>( maRows, nIndex, nCount );
-    updateRows();
-    setModified(true);
+        // remove the rows
+        remove_range<RowVector,RowVector::iterator>( maRows, nIndex, nCount );
+        updateRows();
+        setModified(true);
+    }
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
 void TableModel::UndoRemoveRows( sal_Int32 nIndex, RowVector& aRows )
 {
-    TableModelNotifyGuard aGuard( this );
+    {
+        TableModelNotifyGuard aGuard( this );
 
-    const sal_Int32 nCount = sal::static_int_cast< sal_Int32 >( aRows.size() );
+        const sal_Int32 nCount = sal::static_int_cast< sal_Int32 >( aRows.size() );
 
-    nIndex = insert_range<RowVector,RowVector::iterator,TableRowRef>( maRows, nIndex, nCount );
+        nIndex = insert_range<RowVector,RowVector::iterator,TableRowRef>( maRows, nIndex, nCount );
 
-    for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
-        maRows[nIndex+nOffset] = aRows[nOffset];
+        for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
+            maRows[nIndex+nOffset] = aRows[nOffset];
 
-    updateRows();
-    setModified(true);
+        updateRows();
+        setModified(true);
+    }
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
 void TableModel::UndoInsertColumns( sal_Int32 nIndex, sal_Int32 nCount )
 {
-    TableModelNotifyGuard aGuard( this );
+    {
+        TableModelNotifyGuard aGuard( this );
 
-    // now remove the columns
-    remove_range<ColumnVector,ColumnVector::iterator>( maColumns, nIndex, nCount );
-    sal_Int32 nRows = getRowCountImpl();
-    while( nRows-- )
-        maRows[nRows]->removeColumns( nIndex, nCount );
+        // now remove the columns
+        remove_range<ColumnVector,ColumnVector::iterator>( maColumns, nIndex, nCount );
+        sal_Int32 nRows = getRowCountImpl();
+        while( nRows-- )
+            maRows[nRows]->removeColumns( nIndex, nCount );
 
-    updateColumns();
-    setModified(true);
+        updateColumns();
+        setModified(true);
+    }
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
 void TableModel::UndoRemoveColumns( sal_Int32 nIndex, ColumnVector& aCols, CellVector& aCells )
 {
-    TableModelNotifyGuard aGuard( this );
+    {
+        TableModelNotifyGuard aGuard( this );
 
-    const sal_Int32 nCount = sal::static_int_cast< sal_Int32 >( aCols.size() );
+        const sal_Int32 nCount = sal::static_int_cast< sal_Int32 >( aCols.size() );
 
-    // assert if there are not enough cells saved
-    DBG_ASSERT( (aCols.size() * maRows.size()) == aCells.size(), "sdr::table::TableModel::UndoRemoveColumns(), invalid undo data!" );
+        // assert if there are not enough cells saved
+        DBG_ASSERT( (aCols.size() * maRows.size()) == aCells.size(), "sdr::table::TableModel::UndoRemoveColumns(), invalid undo data!" );
 
-    nIndex = insert_range<ColumnVector,ColumnVector::iterator,TableColumnRef>( maColumns, nIndex, nCount );
-    for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
-        maColumns[nIndex+nOffset] = aCols[nOffset];
+        nIndex = insert_range<ColumnVector,ColumnVector::iterator,TableColumnRef>( maColumns, nIndex, nCount );
+        for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
+            maColumns[nIndex+nOffset] = aCols[nOffset];
 
-    CellVector::iterator aIter( aCells.begin() );
+        CellVector::iterator aIter( aCells.begin() );
 
-    sal_Int32 nRows = getRowCountImpl();
-    for( sal_Int32 nRow = 0; nRow < nRows; ++nRow )
-    {
-        CellVector::iterator aIter2 = aIter + nRow * nCount;
-        OSL_ENSURE(aIter2 < aCells.end(), "invalid iterator!");
-        maRows[nRow]->insertColumns( nIndex, nCount, &aIter2 );
-    }
+        sal_Int32 nRows = getRowCountImpl();
+        for( sal_Int32 nRow = 0; nRow < nRows; ++nRow )
+        {
+            CellVector::iterator aIter2 = aIter + nRow * nCount;
+            OSL_ENSURE(aIter2 < aCells.end(), "invalid iterator!");
+            maRows[nRow]->insertColumns( nIndex, nCount, &aIter2 );
+        }
 
-    updateColumns();
-    setModified(true);
+        updateColumns();
+        setModified(true);
+    }
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
@@ -592,11 +604,25 @@ void TableModel::insertColumns( sal_Int32 nIndex, sal_Int32 nCount )
     if( !(nCount && mpTableObj) )
         return;
 
+    sal_Int32 nInsertedIndex = -1;
+    sal_Int32 nFormatSourceColumn = -1;
+
     try
     {
         SdrModel& rModel(mpTableObj->getSdrModelFromSdrObject());
         TableModelNotifyGuard aGuard( this );
+        const bool bInheritColumn = !maColumns.empty();
+        const sal_Int32 nSourceColumn = bInheritColumn
+            ? std::min(std::max(nIndex, sal_Int32(0)), getColumnCountImpl() - 1)
+            : 0;
+        const sal_Int32 nInheritedWidth
+            = bInheritColumn ? maColumns[nSourceColumn]->mnWidth : 0;
+        const bool bInheritedOptimalWidth
+            = bInheritColumn && maColumns[nSourceColumn]->mbOptimalWidth;
         nIndex = insert_range<ColumnVector,ColumnVector::iterator,TableColumnRef>( maColumns, nIndex, nCount );
+        nInsertedIndex = nIndex;
+        nFormatSourceColumn
+            = nSourceColumn >= nIndex ? nSourceColumn + nCount : nSourceColumn;
 
         sal_Int32 nRows = getRowCountImpl();
         while( nRows-- )
@@ -606,6 +632,11 @@ void TableModel::insertColumns( sal_Int32 nIndex, sal_Int32 nCount )
         for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
         {
             TableColumnRef xNewCol( new TableColumn( this, nIndex+nOffset ) );
+            if (bInheritColumn)
+            {
+                xNewCol->mnWidth = nInheritedWidth;
+                xNewCol->mbOptimalWidth = bInheritedOptimalWidth;
+            }
             maColumns[nIndex+nOffset] = xNewCol;
             aNewColumns[nOffset] = xNewCol;
         }
@@ -615,6 +646,8 @@ void TableModel::insertColumns( sal_Int32 nIndex, sal_Int32 nCount )
         if( bUndo )
         {
             rModel.BegUndo( SvxResId(STR_TABLE_INSCOL) );
+            // Geometry must precede the structural action: undo groups run in
+            // reverse order, so the exact pre-edit rectangle is restored last.
             rModel.AddUndo( rModel.GetSdrUndoFactory().CreateUndoGeoObject(*mpTableObj) );
 
             TableModelRef xThis( this );
@@ -662,6 +695,23 @@ void TableModel::insertColumns( sal_Int32 nIndex, sal_Int32 nCount )
     }
     updateColumns();
     setModified(true);
+    mpTableObj->NbcSyncLogicRectToTableLayout();
+    // XTableColumns::insertByIndex() is the programmatic counterpart of the
+    // Impress UI command. Complete geometry first, then inherit the nearest
+    // original column's cell formatting so both paths keep the same appearance.
+    if (nInsertedIndex >= 0 && nFormatSourceColumn >= 0)
+    {
+        for (sal_Int32 nRow = 0; nRow < getRowCountImpl(); ++nRow)
+        {
+            const CellRef xSourceCell(getCell(nFormatSourceColumn, nRow));
+            for (sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset)
+            {
+                const CellRef xTargetCell(getCell(nInsertedIndex + nOffset, nRow));
+                if (xTargetCell.is() && xSourceCell.is())
+                    xTargetCell->copyFormatFrom(xSourceCell, false);
+            }
+        }
+    }
 }
 
 
@@ -687,6 +737,7 @@ void TableModel::removeColumns( sal_Int32 nIndex, sal_Int32 nCount )
         if( bUndo  )
         {
             rModel.BegUndo( SvxResId(STR_UNDO_COL_DELETE) );
+            // See insertColumns(): structural undo runs first, geometry last.
             rModel.AddUndo( rModel.GetSdrUndoFactory().CreateUndoGeoObject(*mpTableObj) );
         }
 
@@ -774,6 +825,7 @@ void TableModel::removeColumns( sal_Int32 nIndex, sal_Int32 nCount )
 
     updateColumns();
     setModified(true);
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
@@ -782,20 +834,38 @@ void TableModel::insertRows( sal_Int32 nIndex, sal_Int32 nCount )
     if( !(nCount && mpTableObj) )
         return;
 
+    sal_Int32 nInsertedIndex = -1;
+    sal_Int32 nFormatSourceRow = -1;
+
     SdrModel& rModel(mpTableObj->getSdrModelFromSdrObject());
     const bool bUndo(mpTableObj->IsInserted() && rModel.IsUndoEnabled());
 
     try
     {
         TableModelNotifyGuard aGuard( this );
+        const bool bInheritRow = !maRows.empty();
+        const sal_Int32 nSourceRow = bInheritRow
+            ? std::min(std::max(nIndex, sal_Int32(0)), getRowCountImpl() - 1)
+            : 0;
+        const sal_Int32 nInheritedHeight
+            = bInheritRow ? maRows[nSourceRow]->mnHeight : 0;
+        const bool bInheritedOptimalHeight
+            = bInheritRow && maRows[nSourceRow]->mbOptimalHeight;
 
         nIndex = insert_range<RowVector,RowVector::iterator,TableRowRef>( maRows, nIndex, nCount );
+        nInsertedIndex = nIndex;
+        nFormatSourceRow = nSourceRow >= nIndex ? nSourceRow + nCount : nSourceRow;
 
         RowVector aNewRows(nCount);
         const sal_Int32 nColCount = getColumnCountImpl();
         for( sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset )
         {
             TableRowRef xNewRow( new TableRow( this, nIndex+nOffset, nColCount ) );
+            if (bInheritRow)
+            {
+                xNewRow->mnHeight = nInheritedHeight;
+                xNewRow->mbOptimalHeight = bInheritedOptimalHeight;
+            }
             maRows[nIndex+nOffset] = xNewRow;
             aNewRows[nOffset] = xNewRow;
         }
@@ -803,6 +873,8 @@ void TableModel::insertRows( sal_Int32 nIndex, sal_Int32 nCount )
         if( bUndo )
         {
             rModel.BegUndo( SvxResId(STR_TABLE_INSROW) );
+            // Geometry must precede the structural action: undo groups run in
+            // reverse order, so the exact pre-edit rectangle is restored last.
             rModel.AddUndo( rModel.GetSdrUndoFactory().CreateUndoGeoObject(*mpTableObj) );
             TableModelRef xThis( this );
             rModel.AddUndo( std::make_unique<InsertRowUndo>( xThis, nIndex, aNewRows ) );
@@ -836,6 +908,23 @@ void TableModel::insertRows( sal_Int32 nIndex, sal_Int32 nCount )
 
     updateRows();
     setModified(true);
+    mpTableObj->NbcSyncLogicRectToTableLayout();
+    // Match manual row insertion without marking the partially assembled
+    // table modified before its outer geometry has been synchronized.
+    if (nInsertedIndex >= 0 && nFormatSourceRow >= 0)
+    {
+        const sal_Int32 nColumnCount = getColumnCountImpl();
+        for (sal_Int32 nColumn = 0; nColumn < nColumnCount; ++nColumn)
+        {
+            const CellRef xSourceCell(getCell(nColumn, nFormatSourceRow));
+            for (sal_Int32 nOffset = 0; nOffset < nCount; ++nOffset)
+            {
+                const CellRef xTargetCell(getCell(nColumn, nInsertedIndex + nOffset));
+                if (xTargetCell.is() && xSourceCell.is())
+                    xTargetCell->copyFormatFrom(xSourceCell, false);
+            }
+        }
+    }
 }
 
 
@@ -860,6 +949,7 @@ void TableModel::removeRows( sal_Int32 nIndex, sal_Int32 nCount )
         if( bUndo )
         {
             rModel.BegUndo( SvxResId(STR_UNDO_ROW_DELETE) );
+            // See insertRows(): structural undo runs first, geometry last.
             rModel.AddUndo( rModel.GetSdrUndoFactory().CreateUndoGeoObject(*mpTableObj) );
         }
 
@@ -934,6 +1024,7 @@ void TableModel::removeRows( sal_Int32 nIndex, sal_Int32 nCount )
 
     updateRows();
     setModified(true);
+    mpTableObj->NbcSyncLogicRectToTableLayout();
 }
 
 
diff --git a/svx/source/table/tablerow.cxx b/svx/source/table/tablerow.cxx
index 4545642e3..c63663341 100644
--- a/svx/source/table/tablerow.cxx
+++ b/svx/source/table/tablerow.cxx
@@ -289,6 +289,7 @@ void SAL_CALL TableRow::setFastPropertyValue( sal_Int32 nHandle, const Any& aVal
             rModel.AddUndo( std::move(pUndo) );
         }
         mxTableModel->setModified(true);
+        rTableObj.NbcSyncLogicRectToTableLayout();
     }
 }
 
diff --git a/svx/source/table/tableundo.cxx b/svx/source/table/tableundo.cxx
index e5c3b7243..8e08cc1f2 100644
--- a/svx/source/table/tableundo.cxx
+++ b/svx/source/table/tableundo.cxx
@@ -388,6 +388,7 @@ void TableColumnUndo::setData( const Data& rData )
 
     // Trigger re-layout of the table.
     mxCol->getModel()->setModified(true);
+    mxCol->getModel()->getSdrTableObj()->NbcSyncLogicRectToTableLayout();
 }
 
 
@@ -454,6 +455,7 @@ void TableRowUndo::setData( const Data& rData )
 
     // Trigger re-layout of the table.
     mxRow->getModel()->setModified(true);
+    mxRow->getModel()->getSdrTableObj()->NbcSyncLogicRectToTableLayout();
  }
 
 
diff --git a/svx/source/unodraw/unoshtxt.cxx b/svx/source/unodraw/unoshtxt.cxx
index d3837e187..64da139db 100644
--- a/svx/source/unodraw/unoshtxt.cxx
+++ b/svx/source/unodraw/unoshtxt.cxx
@@ -775,16 +775,32 @@ void SvxTextEditSourceImpl::UpdateData()
             SdrTextObj* pTextObj = DynCastSdrTextObj( mpObject );
             if( pTextObj )
             {
-                if( (mpOutliner->GetParagraphCount() == 1 && mpOutliner->GetEditEngine().GetTextLen( 0 ) == 0 )
-                        || (mpOutliner->GetParagraphCount() == 2 && mpOutliner->GetEditEngine().GetTextLen( 0 ) == 0
-                            && mpOutliner->GetEditEngine().GetTextLen( 1 ) == 0) )
+                const bool bEmptyText
+                    = (mpOutliner->GetParagraphCount() == 1
+                       && mpOutliner->GetEditEngine().GetTextLen(0) == 0)
+                      || (mpOutliner->GetParagraphCount() == 2
+                          && mpOutliner->GetEditEngine().GetTextLen(0) == 0
+                          && mpOutliner->GetEditEngine().GetTextLen(1) == 0);
+                std::optional<OutlinerParaObject> pUpdatedText;
+                if (!bEmptyText)
+                    pUpdatedText = mpOutliner->CreateParaObject();
+
+                // Background UNO text cursors commit through this edit source,
+                // bypassing Cell::setString() and Cell::setPropertyValue().
+                // Capture the complete cell before the changed paragraph object
+                // is installed so formatting a complete table-cell text range
+                // participates in the document Undo transaction.
+                if (auto* pCell = dynamic_cast<sdr::table::Cell*>(mpText))
                 {
-                    pTextObj->NbcSetOutlinerParaObjectForText( std::nullopt, mpText );
-                }
-                else
-                {
-                    pTextObj->NbcSetOutlinerParaObjectForText( mpOutliner->CreateParaObject(), mpText );
+                    const OutlinerParaObject* pCurrentText = pCell->GetOutlinerParaObject();
+                    const bool bChanged
+                        = pCurrentText
+                              ? (!pUpdatedText || !(*pCurrentText == *pUpdatedText))
+                              : pUpdatedText.has_value();
+                    if (bChanged)
+                        pCell->AddUndo();
                 }
+                pTextObj->NbcSetOutlinerParaObjectForText(std::move(pUpdatedText), mpText);
             }
 
             if( mpObject->IsEmptyPresObj() )
