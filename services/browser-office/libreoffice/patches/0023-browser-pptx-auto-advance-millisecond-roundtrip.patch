diff --git a/oox/source/ppt/slidetransition.cxx b/oox/source/ppt/slidetransition.cxx
--- a/oox/source/ppt/slidetransition.cxx
+++ b/oox/source/ppt/slidetransition.cxx
@@ -80,6 +80,7 @@ void SlideTransition::setSlideProperties( PropertyMap & aProps )
             aProps.setProperty( PROP_TransitionFadeColor, mnTransitionFadeColor);
             if( mnAdvanceTime != -1 ) {
-                aProps.setProperty( PROP_Duration, mnAdvanceTime/1000);
+                // OOXML advTm is milliseconds; Duration truncates to whole seconds.
+                aProps.setProperty( PROP_HighResDuration, mnAdvanceTime / 1000.0);
                 aProps.setProperty( PROP_Change, static_cast<sal_Int32>(1));
             }
         }
diff --git a/oox/source/ppt/slidetransitioncontext.cxx b/oox/source/ppt/slidetransitioncontext.cxx
--- a/oox/source/ppt/slidetransitioncontext.cxx
+++ b/oox/source/ppt/slidetransitioncontext.cxx
@@ -54,5 +54,8 @@ SlideTransitionContext::SlideTransitionContext( FragmentHandler2 const & rParent,
     // careful. if missing, no auto advance... 0 looks like a valid value
     // for auto advance
     if(rAttribs.hasAttribute( XML_advTm ))
+    {
         maTransition.setOoxAdvanceTime( rAttribs.getInteger( XML_advTm, -1 ) );
+        mbHasTransition = true; // Timing-only <p:transition> has no effect child.
+    }
 }
diff --git a/oox/source/token/properties.txt b/oox/source/token/properties.txt
--- a/oox/source/token/properties.txt
+++ b/oox/source/token/properties.txt
@@ -625,3 +625,4 @@
 XLA1Representation
 ZoomType
 ZoomValue
+HighResDuration
diff --git a/sd/source/filter/eppt/pptx-epptooxml.cxx b/sd/source/filter/eppt/pptx-epptooxml.cxx
--- a/sd/source/filter/eppt/pptx-epptooxml.cxx
+++ b/sd/source/filter/eppt/pptx-epptooxml.cxx
@@ -819,15 +819,21 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
         }
     }
 
-    // check if we resolved what transition to export or time is set
-    if (!nPPTTransitionType && !bOOXmlSpecificTransition && !isTransitionDurationSet)
-        return;
-
     if (ImplGetPropertyValue(mXPagePropSet, "Change"))
         mAny >>= changeType;
 
-    // 1 means automatic, 2 half automatic - not sure what it means - at least I don't see it in UI
-    if (changeType == 1 && ImplGetPropertyValue(mXPagePropSet, "Duration"))
-        mAny >>= advanceTiming;
+    // advTm is the slide's auto-advance time in milliseconds, independent of
+    // whether a visual transition effect is selected.
+    if (changeType == 1 && ImplGetPropertyValue(mXPagePropSet, "HighResDuration"))
+    {
+        double fAdvanceSeconds = -1.0;
+        if ((mAny >>= fAdvanceSeconds) && fAdvanceSeconds >= 0.0
+            && fAdvanceSeconds <= 2147483.0)
+            advanceTiming = static_cast<sal_Int32>(std::round(fAdvanceSeconds * 1000.0));
+    }
+
+    if (!nPPTTransitionType && !bOOXmlSpecificTransition && !isTransitionDurationSet
+        && advanceTiming < 0)
+        return;
 
     if (!bOOXmlSpecificTransition)
@@ -940,5 +946,5 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
             pFS->startElementNS(XML_p, XML_transition,
                 XML_spd, speed,
-                XML_advTm, OString::number(advanceTiming * 1000),
+                XML_advTm, OString::number(advanceTiming),
                 FSNS(XML_p14, XML_dur), OString::number(nTransitionDuration));
         }
@@ -953,4 +959,4 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
             pFS->startElementNS(XML_p, XML_transition,
                 XML_spd, speed,
-                XML_advTm, OString::number(advanceTiming * 1000));
+                XML_advTm, OString::number(advanceTiming));
         }
@@ -992,5 +998,5 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
     pFS->startElementNS(XML_p, XML_transition,
         XML_spd, speed,
-        XML_advTm, sax_fastparser::UseIf(OString::number(advanceTiming * 1000), isAdvanceTimingSet));
+        XML_advTm, sax_fastparser::UseIf(OString::number(advanceTiming), isAdvanceTimingSet));
 
     if (nTransition)
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -2130,3 +2130,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
     xPageProperties->setPropertyValue("DateTimeFormat", uno::Any(sal_Int32(0)));
     xPageProperties->setPropertyValue("HighResDuration", uno::Any(1.0));
+    xPageProperties->setPropertyValue("Change", uno::Any(sal_Int32(0)));
     xPageProperties->setPropertyValue("IsBackgroundObjectsVisible", uno::Any(true));
@@ -2152,3 +2153,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
     xPageProperties->setPropertyValue("DateTimeFormat", uno::Any(sal_Int32(18)));
     xPageProperties->setPropertyValue("HighResDuration", uno::Any(12.5));
+    xPageProperties->setPropertyValue("Change", uno::Any(sal_Int32(1)));
     xPageProperties->setPropertyValue("IsBackgroundObjectsVisible", uno::Any(false));
@@ -2187,5 +2189,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
         CPPUNIT_ASSERT_EQUAL(sal_Int32(18), nValue);
         CPPUNIT_ASSERT(xPageProperties->getPropertyValue("HighResDuration") >>= fValue);
-        CPPUNIT_ASSERT_DOUBLES_EQUAL(12.5, fValue, 0.001);
+        CPPUNIT_ASSERT_DOUBLES_EQUAL_MESSAGE(pPhase, 12.5, fValue, 0.001);
+        CPPUNIT_ASSERT(xPageProperties->getPropertyValue("Change") >>= nValue);
+        CPPUNIT_ASSERT_EQUAL(sal_Int32(1), nValue);
         CPPUNIT_ASSERT(xPageProperties->getPropertyValue("IsBackgroundObjectsVisible")
                        >>= bValue);
@@ -2251,4 +2255,6 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
     CPPUNIT_ASSERT(xPageProperties->getPropertyValue("HighResDuration") >>= fValue);
     CPPUNIT_ASSERT_DOUBLES_EQUAL(1.0, fValue, 0.001);
+    CPPUNIT_ASSERT(xPageProperties->getPropertyValue("Change") >>= nValue);
+    CPPUNIT_ASSERT_EQUAL(sal_Int32(0), nValue);
     CPPUNIT_ASSERT(xPageProperties->getPropertyValue("IsBackgroundObjectsVisible")
                    >>= bValue);
diff --git a/sd/source/ui/unoidl/unopage.cxx b/sd/source/ui/unoidl/unopage.cxx
--- a/sd/source/ui/unoidl/unopage.cxx
+++ b/sd/source/ui/unoidl/unopage.cxx
@@ -158,4 +158,5 @@ struct PageMetadataState
     sd::HeaderFooterSettings maHeaderFooter;
     double mfHighResolutionDuration = 0.0;
+    PresChange mePresChange = PresChange::Manual;
     bool mbBackgroundObjectsVisible = false;
 
@@ -165,4 +166,5 @@ struct PageMetadataState
         return maHeaderFooter == rOther.maHeaderFooter
                && mfHighResolutionDuration == rOther.mfHighResolutionDuration
+               && mePresChange == rOther.mePresChange
                && mbBackgroundObjectsVisible == rOther.mbBackgroundObjectsVisible;
     }
@@ -180,5 +182,6 @@ PageMetadataState getPageMetadata(SdDrawDocument& rDocument, SdPage& rPage)
     return {
         rPage.getHeaderFooterSettings(),
         rPage.GetTime(),
+        rPage.GetPresChange(),
         rPage.TRG_GetMasterPageVisibleLayers().IsSet(aBackgroundObjects),
     };
@@ -188,4 +191,5 @@ void setPageMetadata(SdDrawDocument& rDocument, SdPage& rPage,
 {
     rPage.setHeaderFooterSettings(rState.maHeaderFooter);
     rPage.SetTime(rState.mfHighResolutionDuration);
+    rPage.SetPresChange(rState.mePresChange);
     SdrLayerAdmin& rLayerAdmin = rDocument.GetLayerAdmin();
@@ -717,4 +721,5 @@ void SAL_CALL SdGenericDrawPage::setPropertyValue( const OUString& aPropertyName
         switch (pEntry->nWID)
         {
+            case WID_PAGE_CHANGE:
             case WID_PAGE_HIGHRESDURATION:
             case WID_PAGE_BACKOBJVIS:
