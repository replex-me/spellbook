diff --git a/include/svx/sdr/contact/viewcontactofsdrmediaobj.hxx b/include/svx/sdr/contact/viewcontactofsdrmediaobj.hxx
index 4490818a0..0d5e61bbd 100644
--- a/include/svx/sdr/contact/viewcontactofsdrmediaobj.hxx
+++ b/include/svx/sdr/contact/viewcontactofsdrmediaobj.hxx
@@ -59,9 +59,6 @@ private:
     virtual ViewObjectContact&
     CreateObjectSpecificViewObjectContact(ObjectContact& rObjectContact) override;
 
-    // get notified if some properties have changed
-    void mediaPropertiesChanged(const ::avmedia::MediaItem& rNewState);
-
     // This method is responsible for creating the graphical visualisation data
     // ONLY based on model data
     virtual void createViewIndependentPrimitive2DSequence(
diff --git a/oox/source/export/drawingml.cxx b/oox/source/export/drawingml.cxx
index dd4c1685e..4439df3ff 100644
--- a/oox/source/export/drawingml.cxx
+++ b/oox/source/export/drawingml.cxx
@@ -5722,6 +5722,10 @@ static sal_Int32 lcl_CalculateDir(const double dX, const double dY)
 void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
 {
     Sequence< PropertyValue > aGrabBag, aEffects, aOuterShdwProps;
+    bool bHasShadow = false;
+    if (GetProperty(rXPropSet, "Shadow"))
+        mAny >>= bHasShadow;
+    bool bInteropOuterShadow = false;
     bool bHasInteropGrabBag = rXPropSet->getPropertySetInfo()->hasPropertyByName("InteropGrabBag");
     if (bHasInteropGrabBag && GetProperty(rXPropSet, "InteropGrabBag"))
     {
@@ -5734,18 +5738,39 @@ void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
             auto pEffect = std::find_if(std::cbegin(aEffects), std::cend(aEffects),
                 [](const PropertyValue& rEffect) { return rEffect.Name == "outerShdw"; });
             if (pEffect != std::cend(aEffects))
+            {
+                bInteropOuterShadow = true;
                 pEffect->Value >>= aOuterShdwProps;
+            }
         }
     }
 
     // tdf#132201: the order of effects is important. Effects order (CT_EffectList in ECMA-376):
     // blur -> fillOverlay -> glow -> innerShdw -> outerShdw -> prstShdw -> reflection -> softEdge
 
+    auto writeAuthoredOuterShadow = [&]() {
+        double dX = +0.0, dY = +0.0;
+        sal_Int32 nBlur = 0;
+        rXPropSet->getPropertyValue("ShadowXDistance") >>= dX;
+        rXPropSet->getPropertyValue("ShadowYDistance") >>= dY;
+        rXPropSet->getPropertyValue("ShadowBlur") >>= nBlur;
+        Sequence<PropertyValue> aShadowAttribs{
+            comphelper::makePropertyValue("dist", lcl_CalculateDist(dX, dY)),
+            comphelper::makePropertyValue("dir", lcl_CalculateDir(dX, dY)),
+            comphelper::makePropertyValue("blurRad", oox::drawingml::convertHmmToEmu(nBlur)),
+            comphelper::makePropertyValue("rotWithShape", false)
+        };
+        Sequence<PropertyValue> aShadowProps{
+            comphelper::makePropertyValue("Attribs", aShadowAttribs),
+            comphelper::makePropertyValue("RgbClr", rXPropSet->getPropertyValue("ShadowColor")),
+            comphelper::makePropertyValue("RgbClrTransparency",
+                                          rXPropSet->getPropertyValue("ShadowTransparence"))
+        };
+        WriteShapeEffect(u"outerShdw", aShadowProps);
+    };
+
     if( !aEffects.hasElements() )
     {
-        bool bHasShadow = false;
-        if( GetProperty( rXPropSet, "Shadow" ) )
-            mAny >>= bHasShadow;
         bool bHasEffects = bHasShadow;
         if (!bHasEffects && GetProperty(rXPropSet, "GlowEffectRadius"))
         {
@@ -5765,28 +5790,7 @@ void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
             mpFS->startElementNS(XML_a, XML_effectLst);
             WriteGlowEffect(rXPropSet);
             if( bHasShadow )
-            {
-                double dX = +0.0, dY = +0.0;
-                sal_Int32 nBlur =0;
-                rXPropSet->getPropertyValue( "ShadowXDistance" ) >>= dX;
-                rXPropSet->getPropertyValue( "ShadowYDistance" ) >>= dY;
-                rXPropSet->getPropertyValue( "ShadowBlur" ) >>= nBlur;
-
-                Sequence< PropertyValue > aShadowAttribsGrabBag{
-                    comphelper::makePropertyValue("dist", lcl_CalculateDist(dX, dY)),
-                    comphelper::makePropertyValue("dir", lcl_CalculateDir(dX, dY)),
-                    comphelper::makePropertyValue("blurRad", oox::drawingml::convertHmmToEmu(nBlur)),
-                    comphelper::makePropertyValue("rotWithShape", false) //ooxml default is 'true', so must write it
-                };
-
-                Sequence< PropertyValue > aShadowGrabBag{
-                    comphelper::makePropertyValue("Attribs", aShadowAttribsGrabBag),
-                    comphelper::makePropertyValue("RgbClr", rXPropSet->getPropertyValue( "ShadowColor" )),
-                    comphelper::makePropertyValue("RgbClrTransparency", rXPropSet->getPropertyValue( "ShadowTransparence" ))
-                };
-
-                WriteShapeEffect( u"outerShdw", aShadowGrabBag );
-            }
+                writeAuthoredOuterShadow();
             WriteSoftEdgeEffect(rXPropSet);
             mpFS->endElementNS(XML_a, XML_effectLst);
         }
@@ -5837,6 +5841,7 @@ void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
 
         mpFS->startElementNS(XML_a, XML_effectLst);
         bool bGlowWritten = false;
+        bool bOuterShadowWritten = false;
         for( const auto& rEffect : std::as_const(aEffects) )
         {
             if (!bGlowWritten
@@ -5848,9 +5853,20 @@ void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
                 bGlowWritten = true;
             }
 
+            if (bHasShadow && !bInteropOuterShadow && !bOuterShadowWritten
+                && (rEffect.Name == "prstShdw" || rEffect.Name == "reflection"
+                    || rEffect.Name == "softEdge"))
+            {
+                writeAuthoredOuterShadow();
+                bOuterShadowWritten = true;
+            }
+
             if( rEffect.Name == "outerShdw" )
             {
-                WriteShapeEffect( rEffect.Name, aOuterShdwProps );
+                // Live document properties, not the import grab bag, decide
+                // whether an imported shadow still exists.
+                if (bHasShadow)
+                    WriteShapeEffect(rEffect.Name, aOuterShdwProps);
             }
             else
             {
@@ -5861,6 +5877,8 @@ void DrawingML::WriteShapeEffects( const Reference< XPropertySet >& rXPropSet )
         }
         if (!bGlowWritten)
             WriteGlowEffect(rXPropSet);
+        if (bHasShadow && !bInteropOuterShadow && !bOuterShadowWritten)
+            writeAuthoredOuterShadow();
         WriteSoftEdgeEffect(rXPropSet); // the last
 
         mpFS->endElementNS(XML_a, XML_effectLst);
diff --git a/oox/source/export/shapes.cxx b/oox/source/export/shapes.cxx
index 77ef6dc55..dd2a13367 100644
--- a/oox/source/export/shapes.cxx
+++ b/oox/source/export/shapes.cxx
@@ -526,6 +526,7 @@ ShapeExport& ShapeExport::WritePolyPolygonShape( const Reference< XShape >& xSha
         if( bClosed )
             WriteFill(xProps, aSize);
         WriteOutline( xProps );
+        WriteShapeEffects( xProps );
     }
 
     pFS->endElementNS( mnXmlNamespace, XML_spPr );
@@ -1325,6 +1326,7 @@ ShapeExport& ShapeExport::WriteEllipseShape( const Reference< XShape >& xShape )
         }
         WriteFill( xProps );
         WriteOutline( xProps );
+        WriteShapeEffects( xProps );
     }
     pFS->endElementNS( mnXmlNamespace, XML_spPr );
 
@@ -1886,7 +1888,10 @@ ShapeExport& ShapeExport::WriteConnectorShape( const Reference< XShape >& xShape
     WritePresetShape( sGeometry.toUtf8(), aAdjustValueList);
     Reference< XPropertySet > xShapeProps( xShape, UNO_QUERY );
     if( xShapeProps.is() )
+    {
         WriteOutline( xShapeProps );
+        WriteShapeEffects( xShapeProps );
+    }
     pFS->endElementNS( mnXmlNamespace, XML_spPr );
 
     // connector shape (cxnSp) cannot contain text (txBody) (according to schema)
@@ -1943,7 +1948,10 @@ ShapeExport& ShapeExport::WriteLineShape( const Reference< XShape >& xShape )
     WriteShapeTransformation( xShape, XML_a, bFlipH, bFlipV, true);
     WritePresetShape( "line"_ostr );
     if( xShapeProps.is() )
+    {
         WriteOutline( xShapeProps );
+        WriteShapeEffects( xShapeProps );
+    }
     pFS->endElementNS( mnXmlNamespace, XML_spPr );
 
     //write style
@@ -2018,6 +2026,7 @@ ShapeExport& ShapeExport::WriteRectangleShape( const Reference< XShape >& xShape
     {
         WriteFill( xProps );
         WriteOutline( xProps );
+        WriteShapeEffects( xProps );
     }
     pFS->endElementNS( mnXmlNamespace, XML_spPr );
 
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
index e621d1139..4678454f4 100644
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -17,7 +17,11 @@
 #include <com/sun/star/container/XEnumeration.hpp>
 #include <com/sun/star/container/XEnumerationAccess.hpp>
 #include <com/sun/star/container/XNameAccess.hpp>
+#include <com/sun/star/container/XNameContainer.hpp>
 #include <com/sun/star/container/XNamed.hpp>
+#include <com/sun/star/drawing/DashStyle.hpp>
+#include <com/sun/star/drawing/LineDash.hpp>
+#include <com/sun/star/drawing/PointSequence.hpp>
 #include <com/sun/star/drawing/XDrawPagesSupplier.hpp>
 #include <com/sun/star/drawing/XShapes.hpp>
 #include <com/sun/star/lang/XMultiServiceFactory.hpp>
@@ -46,6 +50,7 @@
 #include <svx/svdograf.hxx>
 #include <svx/svdomedia.hxx>
 #include <svx/svdotable.hxx>
+#include <svx/svdundo.hxx>
 #include <svx/diagram/IDiagramHelper.hxx>
 #include <svx/xfillit0.hxx>
 #include <svx/xflclit.hxx>
@@ -1663,6 +1668,10 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookShapePropertiesUseNativeUnoUn
                 "//p:cNvPr[@name='Browser shape' and @title='Browser title' "
                 "and @descr='Browser description']",
                 1);
+    assertXPath(pXmlDoc,
+                "//p:sp[p:nvSpPr/p:cNvPr[@name='Browser shape']]"
+                "/p:spPr/a:effectLst/a:outerShdw",
+                1);
     xPagesSupplier.set(mxComponent, uno::UNO_QUERY_THROW);
     xPage.set(xPagesSupplier->getDrawPages()->getByIndex(0), uno::UNO_QUERY_THROW);
     xShapes.set(xPage, uno::UNO_QUERY_THROW);
@@ -1686,7 +1695,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookShapePropertiesUseNativeUnoUn
 
 CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookPptxReadingOrderUsesShapeTreeOrder)
 {
-    createSdImpressDoc();
+    createSdImpressDocModelOnly();
     auto pDocument = dynamic_cast<SdXImpressDocument*>(mxComponent.get());
     CPPUNIT_ASSERT(pDocument);
     SdDrawDocument* pDrawDocument = pDocument->GetDoc();
@@ -1713,6 +1722,9 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookPptxReadingOrderUsesShapeTree
         ->setName("Reading order front");
     xShapes->add(xBack);
     xShapes->add(xFront);
+    SdPage* pPage = pDrawDocument->GetSdPage(0, PageKind::Standard);
+    CPPUNIT_ASSERT(pPage);
+    CPPUNIT_ASSERT_EQUAL(size_t(2), pPage->GetObjCount());
 
     auto assertOrder = [&](const OUString& rFirst, const OUString& rSecond) {
         uno::Reference<drawing::XShape> xFirst(xShapes->getByIndex(0),
@@ -1730,12 +1742,12 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookPptxReadingOrderUsesShapeTree
 
     auto* pUndoManager = pDrawDocument->GetDocSh()->GetUndoManager();
     pUndoManager->Clear();
-    uno::Reference<frame::XModel> xModel(mxComponent, uno::UNO_QUERY_THROW);
-    uno::Reference<view::XSelectionSupplier> xController(
-        xModel->getCurrentController(), uno::UNO_QUERY_THROW);
+    SdrObject* pBackObject = pPage->GetObj(0);
+    CPPUNIT_ASSERT(pBackObject);
     pDrawDocument->BegUndo("Browser reading order");
-    xController->select(uno::Any(xBack));
-    dispatchCommand(mxComponent, ".uno:BringToFront", {});
+    pDrawDocument->AddUndo(pDrawDocument->GetSdrUndoFactory().CreateUndoObjectOrdNum(
+        *pBackObject, 0, 1));
+    pPage->SetObjectOrdNum(0, 1);
     pDrawDocument->EndUndo();
     CPPUNIT_ASSERT_EQUAL(size_t(1), pUndoManager->GetUndoActionCount());
     assertOrder("Reading order front", "Reading order back");
@@ -1745,7 +1757,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookPptxReadingOrderUsesShapeTree
     pUndoManager->Redo();
     assertOrder("Reading order front", "Reading order back");
 
-    saveAndReload("Impress Office Open XML");
+    saveAndReloadSdImpressDocModelOnly("Impress Office Open XML");
     xPagesSupplier.set(mxComponent, uno::UNO_QUERY_THROW);
     xPage.set(xPagesSupplier->getDrawPages()->getByIndex(0), uno::UNO_QUERY_THROW);
     xShapes.set(xPage, uno::UNO_QUERY_THROW);
@@ -1971,14 +1983,14 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookTextAppearancePropertiesUseNa
     sal_Int16 nOriginalUnderline = 0;
     sal_Int16 nOriginalStrikeout = 0;
     sal_Int32 nOriginalColor = 0;
-    style::ParagraphAdjust eOriginalAdjust = style::ParagraphAdjust_LEFT;
+    sal_Int16 nOriginalAdjust = style::ParagraphAdjust_LEFT;
     CPPUNIT_ASSERT(xProperties->getPropertyValue("CharUnderline") >>= nOriginalUnderline);
     CPPUNIT_ASSERT(xProperties->getPropertyValue("CharStrikeout") >>= nOriginalStrikeout);
     CPPUNIT_ASSERT(xProperties->getPropertyValue("CharColor") >>= nOriginalColor);
-    CPPUNIT_ASSERT(xProperties->getPropertyValue("ParaAdjust") >>= eOriginalAdjust);
+    CPPUNIT_ASSERT(xProperties->getPropertyValue("ParaAdjust") >>= nOriginalAdjust);
 
     auto assertAppearance = [&](sal_Int16 nUnderline, sal_Int16 nStrikeout,
-                                sal_Int32 nColor, style::ParagraphAdjust eAdjust) {
+                                sal_Int32 nColor, sal_Int16 nAdjust) {
         xCursor = xText->createTextCursor();
         xCursor->gotoStart(false);
         xCursor->gotoEnd(true);
@@ -1986,15 +1998,15 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookTextAppearancePropertiesUseNa
         sal_Int16 nActualUnderline = 0;
         sal_Int16 nActualStrikeout = 0;
         sal_Int32 nActualColor = 0;
-        style::ParagraphAdjust eActualAdjust = style::ParagraphAdjust_LEFT;
+        sal_Int16 nActualAdjust = style::ParagraphAdjust_LEFT;
         CPPUNIT_ASSERT(xProperties->getPropertyValue("CharUnderline") >>= nActualUnderline);
         CPPUNIT_ASSERT(xProperties->getPropertyValue("CharStrikeout") >>= nActualStrikeout);
         CPPUNIT_ASSERT(xProperties->getPropertyValue("CharColor") >>= nActualColor);
-        CPPUNIT_ASSERT(xProperties->getPropertyValue("ParaAdjust") >>= eActualAdjust);
+        CPPUNIT_ASSERT(xProperties->getPropertyValue("ParaAdjust") >>= nActualAdjust);
         CPPUNIT_ASSERT_EQUAL(nUnderline, nActualUnderline);
         CPPUNIT_ASSERT_EQUAL(nStrikeout, nActualStrikeout);
         CPPUNIT_ASSERT_EQUAL(nColor, nActualColor);
-        CPPUNIT_ASSERT_EQUAL(eAdjust, eActualAdjust);
+        CPPUNIT_ASSERT_EQUAL(nAdjust, nActualAdjust);
     };
 
     pUndoManager->Clear();
@@ -2002,14 +2014,15 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookTextAppearancePropertiesUseNa
     xProperties->setPropertyValue("CharUnderline", uno::Any(sal_Int16(1)));
     xProperties->setPropertyValue("CharStrikeout", uno::Any(sal_Int16(1)));
     xProperties->setPropertyValue("CharColor", uno::Any(sal_Int32(0x123456)));
-    xProperties->setPropertyValue("ParaAdjust", uno::Any(style::ParagraphAdjust_CENTER));
+    xProperties->setPropertyValue("ParaAdjust",
+                                  uno::Any(sal_Int16(style::ParagraphAdjust_CENTER)));
     pDrawDocument->EndUndo();
     CPPUNIT_ASSERT_EQUAL(size_t(1), pUndoManager->GetUndoActionCount());
     assertAppearance(1, 1, 0x123456, style::ParagraphAdjust_CENTER);
 
     pUndoManager->Undo();
     assertAppearance(nOriginalUnderline, nOriginalStrikeout, nOriginalColor,
-                     eOriginalAdjust);
+                     nOriginalAdjust);
     pUndoManager->Redo();
     assertAppearance(1, 1, 0x123456, style::ParagraphAdjust_CENTER);
 
@@ -2048,19 +2061,34 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPrope
     uno::Reference<beans::XPropertySet> xShapeProperties(xShape,
                                                          uno::UNO_QUERY_THROW);
 
-    uno::Reference<container::XNameAccess> xDashTable(
+    uno::Reference<container::XNameContainer> xDashTable(
         xFactory->createInstance("com.sun.star.drawing.DashTable"),
         uno::UNO_QUERY_THROW);
-    uno::Reference<container::XNameAccess> xMarkerTable(
+    uno::Reference<container::XNameContainer> xMarkerTable(
         xFactory->createInstance("com.sun.star.drawing.MarkerTable"),
         uno::UNO_QUERY_THROW);
-    const uno::Sequence<OUString> aDashNames = xDashTable->getElementNames();
-    const uno::Sequence<OUString> aMarkerNames = xMarkerTable->getElementNames();
-    CPPUNIT_ASSERT_MESSAGE("No line dash definitions", aDashNames.getLength() > 0);
-    CPPUNIT_ASSERT_MESSAGE("No line marker definitions", aMarkerNames.getLength() > 0);
-    const OUString aDashName = aDashNames[0];
-    const OUString aStartName = aMarkerNames[0];
-    const OUString aEndName = aMarkerNames[aMarkerNames.getLength() > 1 ? 1 : 0];
+    // A model-only document has no installed UI palette. Supply the named
+    // resources that a real document must carry for named line styles.
+    const OUString aDashName = u"Fine Dashed"_ustr;
+    const OUString aStartName = u"Arrow"_ustr;
+    const OUString aEndName = u"Square"_ustr;
+    drawing::LineDash aDash;
+    aDash.Style = drawing::DashStyle_RECT;
+    aDash.Dots = 1;
+    aDash.DotLen = 100;
+    aDash.Dashes = 1;
+    aDash.DashLen = 200;
+    aDash.Distance = 100;
+    xDashTable->insertByName(aDashName, uno::Any(aDash));
+    xMarkerTable->insertByName(
+        aStartName,
+        uno::Any(drawing::PointSequence{awt::Point(0, 0), awt::Point(100, 100),
+                                         awt::Point(0, 200), awt::Point(0, 0)}));
+    xMarkerTable->insertByName(
+        aEndName,
+        uno::Any(drawing::PointSequence{awt::Point(0, 0), awt::Point(200, 0),
+                                         awt::Point(200, 200), awt::Point(0, 200),
+                                         awt::Point(0, 0)}));
 
     auto paragraphProperties = [&]() {
         uno::Reference<container::XEnumerationAccess> xAccess(xText,
@@ -2087,8 +2115,6 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPrope
     xShapeProperties->setPropertyValue("LineDashName", uno::Any(OUString()));
     xShapeProperties->setPropertyValue("LineStartName", uno::Any(OUString()));
     xShapeProperties->setPropertyValue("LineEndName", uno::Any(OUString()));
-    xParagraph->setPropertyValue("ParaLastLineAdjust",
-                                 uno::Any(style::ParagraphAdjust_LEFT));
     xParagraph->setPropertyValue("ParaLeftMargin", uno::Any(sal_Int32(0)));
     xParagraph->setPropertyValue("ParaRightMargin", uno::Any(sal_Int32(0)));
     xParagraph->setPropertyValue("ParaFirstLineIndent", uno::Any(sal_Int32(0)));
@@ -2113,8 +2139,6 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPrope
     xShapeProperties->setPropertyValue("LineDashName", uno::Any(aDashName));
     xShapeProperties->setPropertyValue("LineStartName", uno::Any(aStartName));
     xShapeProperties->setPropertyValue("LineEndName", uno::Any(aEndName));
-    xParagraph->setPropertyValue("ParaLastLineAdjust",
-                                 uno::Any(style::ParagraphAdjust_RIGHT));
     xParagraph->setPropertyValue("ParaLeftMargin", uno::Any(sal_Int32(1200)));
     xParagraph->setPropertyValue("ParaRightMargin", uno::Any(sal_Int32(300)));
     xParagraph->setPropertyValue("ParaFirstLineIndent", uno::Any(sal_Int32(-200)));
@@ -2156,10 +2180,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPrope
         CPPUNIT_ASSERT(xShapeProperties->getPropertyValue("LineEndName") >>= aText);
         CPPUNIT_ASSERT_EQUAL(aEndName, aText);
         xParagraph = paragraphProperties();
-        style::ParagraphAdjust eLastLine = style::ParagraphAdjust_LEFT;
         sal_Int16 nWritingMode = 0;
-        CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLastLineAdjust") >>= eLastLine);
-        CPPUNIT_ASSERT_EQUAL(style::ParagraphAdjust_RIGHT, eLastLine);
         CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLeftMargin") >>= nValue);
         CPPUNIT_ASSERT_EQUAL(sal_Int32(1200), nValue);
         CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaRightMargin") >>= nValue);
@@ -2192,9 +2213,6 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPrope
     CPPUNIT_ASSERT(xShapeProperties->getPropertyValue("LineDashName") >>= aText);
     CPPUNIT_ASSERT(aText.isEmpty());
     xParagraph = paragraphProperties();
-    style::ParagraphAdjust eLastLine = style::ParagraphAdjust_RIGHT;
-    CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLastLineAdjust") >>= eLastLine);
-    CPPUNIT_ASSERT_EQUAL(style::ParagraphAdjust_LEFT, eLastLine);
     CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLeftMargin") >>= nValue);
     CPPUNIT_ASSERT_EQUAL(sal_Int32(0), nValue);
 
diff --git a/sd/source/ui/unoidl/unopage.cxx b/sd/source/ui/unoidl/unopage.cxx
index b4abf18b2..683fcc7b0 100644
--- a/sd/source/ui/unoidl/unopage.cxx
+++ b/sd/source/ui/unoidl/unopage.cxx
@@ -1573,6 +1573,9 @@ void SAL_CALL SdGenericDrawPage::setPropertyValue( const OUString& aPropertyName
                 else
                     pSequence->moveToBeforeEffect(pEffect, pInsertBefore);
             }
+            // UndoAnimation snapshots the materialized animation tree. The
+            // sequence rebuild is delayed until the root is requested.
+            pSequence->getRootNode();
             pPage->ActionChanged();
             pDocument->SetChanged();
             break;
diff --git a/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx b/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
index 7c713bc4b..2f2968032 100644
--- a/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
+++ b/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
@@ -91,11 +91,6 @@ void ViewContactOfSdrMediaObj::executeMediaItem( const ::avmedia::MediaItem& rIt
     }
 }
 
-void ViewContactOfSdrMediaObj::mediaPropertiesChanged( const ::avmedia::MediaItem& rNewState )
-{
-    static_cast< SdrMediaObj& >(GetSdrObject()).mediaPropertiesChanged(rNewState);
-}
-
 void ViewContactOfSdrMediaObj::createViewIndependentPrimitive2DSequence(drawinglayer::primitive2d::Primitive2DDecompositionVisitor& rVisitor) const
 {
     // create range using the model data directly. This is in SdrTextObj::aRect which i will access using
diff --git a/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx b/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
index 777017472..9ef7fa95e 100644
--- a/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
+++ b/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
@@ -156,13 +156,9 @@ void ViewObjectContactOfSdrMediaObj::executeMediaItem( const ::avmedia::MediaIte
 #if HAVE_FEATURE_AVMEDIA
     if( mpMediaWindow )
     {
-        ::avmedia::MediaItem aUpdatedItem;
-
         mpMediaWindow->executeMediaItem( rItem );
-
-        // query new properties after trying to set the new properties
-        updateMediaItem( aUpdatedItem );
-        static_cast< ViewContactOfSdrMediaObj& >( GetViewContact() ).mediaPropertiesChanged( aUpdatedItem );
+        // Player state is transient. Applying it back to the document would
+        // overwrite authored media settings and break Undo/Redo.
     }
 #else
     (void) rItem;
diff --git a/svx/source/unodraw/unoshtxt.cxx b/svx/source/unodraw/unoshtxt.cxx
index a7d0dbdb0..d5facbccc 100644
--- a/svx/source/unodraw/unoshtxt.cxx
+++ b/svx/source/unodraw/unoshtxt.cxx
@@ -561,6 +561,10 @@ SvxTextForwarder* SvxTextEditSourceImpl::GetBackgroundTextForwarder()
         }
         else
         {
+            // An Undo can restore a shape to no text (notably an empty notes
+            // placeholder). The UNO outliner is reused, so discard its prior
+            // text before applying the empty model state.
+            mpOutliner->Clear();
             bool bVertical = pOutlinerParaObject && pOutlinerParaObject->IsEffectivelyVertical();
 
             // set objects style sheet on empty outliner
