diff --git a/oox/source/export/drawingml.cxx b/oox/source/export/drawingml.cxx
--- a/oox/source/export/drawingml.cxx
+++ b/oox/source/export/drawingml.cxx
@@ -3490,7 +3490,12 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
     sal_Int32 nParaLeftMargin = 0;
+    sal_Int32 nParaRightMargin = 0;
     sal_Int32 nParaFirstLineIndent = 0;

     if (GetProperty(rXPropSet, "ParaLeftMargin"))
         mAny >>= nParaLeftMargin;
+    bool bDirectRightMargin = false;
+    if (GetPropertyAndState(rXPropSet, rXPropState, "ParaRightMargin", eState)
+        && (mAny >>= nParaRightMargin))
+        bDirectRightMargin = eState == beans::PropertyState_DIRECT_VALUE;
     if (GetProperty(rXPropSet, "ParaFirstLineIndent"))
         mAny >>= nParaFirstLineIndent;
@@ -3524,6 +3529,8 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
     sal_Int32 nParaDefaultTabSize = 0;
     if (GetProperty(rXPropSet, "ParaTabStopDefaultDistance"))
         mAny >>= nParaDefaultTabSize;
+    const bool bWriteRightMargin = nParaRightMargin >= 0 && nParaRightMargin <= 142240
+        && (nParaRightMargin > 0 || bDirectRightMargin);

     if (nParaLeftMargin) // For Paragraph
         mpFS->startElementNS( XML_a, nElement,
@@ -3531,5 +3538,6 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
                            XML_lvl, sax_fastparser::UseIf(OString::number(nLevel), nLevel > 0),
                            XML_marL, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaLeftMargin)), nParaLeftMargin > 0),
+                           XML_marR, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaRightMargin)), bWriteRightMargin),
                            XML_indent, sax_fastparser::UseIf(OString::number((bForceZeroIndent && nParaFirstLineIndent == 0) ? 0 : oox::drawingml::convertHmmToEmu(nParaFirstLineIndent)), (bForceZeroIndent || nParaFirstLineIndent != 0)),
                            XML_algn, GetAlignment( nAlignment ),
                            XML_defTabSz, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaDefaultTabSize)), nParaDefaultTabSize > 0),
@@ -3540,5 +3548,6 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
                            XML_lvl, sax_fastparser::UseIf(OString::number(nLevel), nLevel > 0),
                            XML_marL, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nLeftMargin)), nLeftMargin > 0),
+                           XML_marR, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaRightMargin)), bWriteRightMargin),
                            XML_indent, sax_fastparser::UseIf(OString::number(!bForceZeroIndent ? oox::drawingml::convertHmmToEmu(nLineIndentation) : 0), (bForceZeroIndent || ( nLineIndentation != 0))),
                            XML_algn, GetAlignment( nAlignment ),
                            XML_defTabSz, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaDefaultTabSize)), nParaDefaultTabSize > 0),
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -2218,9 +2218,17 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
         xParagraph = paragraphProperties();
         sal_Int16 nWritingMode = 0;
-        CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLeftMargin") >>= nValue);
-        CPPUNIT_ASSERT_EQUAL(sal_Int32(1200), nValue);
+        // After import, marL/indent can live in numbering rules instead of
+        // the direct paragraph fields. The saved OOXML is asserted below.
+        if (!bAfterReload)
+        {
+            CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaLeftMargin") >>= nValue);
+            CPPUNIT_ASSERT_EQUAL(sal_Int32(1200), nValue);
+        }
         CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaRightMargin") >>= nValue);
         CPPUNIT_ASSERT_EQUAL(sal_Int32(300), nValue);
-        CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaFirstLineIndent") >>= nValue);
-        CPPUNIT_ASSERT_EQUAL(sal_Int32(-200), nValue);
+        if (!bAfterReload)
+        {
+            CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaFirstLineIndent") >>= nValue);
+            CPPUNIT_ASSERT_EQUAL(sal_Int32(-200), nValue);
+        }
         CPPUNIT_ASSERT(xParagraph->getPropertyValue("ParaTopMargin") >>= nValue);
@@ -2266,5 +2274,8 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testSpellbookMetadataLineAndParagraphPropertiesUseNativeUndo)
     xShape.set(xShapes->getByIndex(0), uno::UNO_QUERY_THROW);
     xShapeProperties.set(xShape, uno::UNO_QUERY_THROW);
     xText.set(xShape, uno::UNO_QUERY_THROW);
+    // Verify the saved OOXML as well as the UNO readback after reopening.
+    xmlDocUniquePtr pSlideXml = parseExport("ppt/slides/slide1.xml");
+    assertXPath(pSlideXml, "//a:pPr[@marL='432000' and @marR='108000']", 1);
     assertChanged("after PPTX reload", true);
 }
