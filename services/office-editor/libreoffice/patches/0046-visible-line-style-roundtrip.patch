diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -1186,8 +1186,10 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagr
         pObject->getUnoShape(), uno::UNO_QUERY_THROW);
     OUString aOldStartName;
     OUString aOldEndName;
+    drawing::LineStyle eOldLineStyle = drawing::LineStyle_SOLID;
     xShapeProperties->getPropertyValue(u"LineStartName"_ustr) >>= aOldStartName;
     xShapeProperties->getPropertyValue(u"LineEndName"_ustr) >>= aOldEndName;
+    xShapeProperties->getPropertyValue(u"LineStyle"_ustr) >>= eOldLineStyle;
 
     auto paragraphProperties = [&]() {
         uno::Reference<text::XText> xText(pObject->getUnoShape(), uno::UNO_QUERY_THROW);
@@ -1228,6 +1230,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagr
                 "IsBackgroundObjectsVisible": false
             }},
             {"SetObjectProperties.0": {
+                "LineStyle": 2,
                 "LineStartName": "Arrow",
                 "LineEndName": "Square"
             }},
@@ -1262,6 +1265,9 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagr
         CPPUNIT_ASSERT(!pPage->TRG_GetMasterPageVisibleLayers().IsSet(aBackgroundObjects));
         OUString aStartName;
         OUString aEndName;
+        drawing::LineStyle eLineStyle = drawing::LineStyle_NONE;
+        xShapeProperties->getPropertyValue(u"LineStyle"_ustr) >>= eLineStyle;
+        CPPUNIT_ASSERT_EQUAL(drawing::LineStyle_DASH, eLineStyle);
         xShapeProperties->getPropertyValue(u"LineStartName"_ustr) >>= aStartName;
         xShapeProperties->getPropertyValue(u"LineEndName"_ustr) >>= aEndName;
         CPPUNIT_ASSERT_EQUAL(u"Arrow"_ustr, aStartName);
@@ -1291,6 +1297,9 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagr
         bOldBackgroundObjects,
         pPage->TRG_GetMasterPageVisibleLayers().IsSet(aBackgroundObjects));
     OUString aCurrentName;
+    drawing::LineStyle eCurrentLineStyle = drawing::LineStyle_NONE;
+    xShapeProperties->getPropertyValue(u"LineStyle"_ustr) >>= eCurrentLineStyle;
+    CPPUNIT_ASSERT_EQUAL(eOldLineStyle, eCurrentLineStyle);
     xShapeProperties->getPropertyValue(u"LineStartName"_ustr) >>= aCurrentName;
     CPPUNIT_ASSERT_EQUAL(aOldStartName, aCurrentName);
     xShapeProperties->getPropertyValue(u"LineEndName"_ustr) >>= aCurrentName;
diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -2125,6 +2125,7 @@ void handleSetObjectProperties(SlideCommandContext& rCtx, const std::string& rKe
         u"SizeProtect"_ustr,
         u"Printable"_ustr,
         u"LineDashName"_ustr,
+        u"LineStyle"_ustr,
         u"LineStartName"_ustr,
         u"LineEndName"_ustr,
         u"FillColor"_ustr,
@@ -2200,6 +2201,9 @@ void handleSetObjectProperties(SlideCommandContext& rCtx, const std::string& rKe
             case cpo::uno::TypeClass_ENUM:
             {
                 const sal_Int32 nValue = rEntry.second.get_value<sal_Int32>();
+                if (aName == u"LineStyle"_ustr && (nValue < 0 || nValue > 2))
+                    throw lang::IllegalArgumentException(u"Line style is out of range"_ustr,
+                                                          nullptr, 0);
                 aValue.setValue(&nValue, xInfo->getPropertyByName(aName).Type);
                 break;
             }
