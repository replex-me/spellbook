diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
index a786ef4c3371d4e4a60df1c489288abc3ff5dfe5..cd07a751a9fd127bec27315d8a1b1eaaf1139cdd 100644
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -22,6 +22,8 @@
 #include <com/sun/star/animations/XAnimationNode.hpp>
 #include <com/sun/star/animations/XTimeContainer.hpp>
 #include <com/sun/star/animations/XAnimationNodeSupplier.hpp>
+#include <com/sun/star/awt/FontSlant.hpp>
+#include <com/sun/star/awt/FontWeight.hpp>
 #include <com/sun/star/container/XEnumeration.hpp>
 #include <com/sun/star/container/XEnumerationAccess.hpp>
 #include <com/sun/star/drawing/XDrawPagesSupplier.hpp>
@@ -47,6 +49,7 @@
 #include <com/sun/star/table/XMergeableCell.hpp>
 #include <com/sun/star/text/GraphicCrop.hpp>
 #include <com/sun/star/text/XText.hpp>
+#include <com/sun/star/text/XTextCursor.hpp>
 #include <com/sun/star/text/WritingMode2.hpp>
 #include <com/sun/star/view/XSelectionSupplier.hpp>
 
@@ -715,6 +718,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedTextFormattingAppliesToAllScripts
 
     pDoc->EnableUndo(false);
     pTextObject->SetText(u"Latin 한글 عربي"_ustr);
+    pTextObject->SetEmptyPresObj(false);
     pDoc->EnableUndo(true);
     const OutlinerParaObject aOriginal(*pTextObject->GetOutlinerParaObject());
 
@@ -724,7 +728,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedTextFormattingAppliesToAllScripts
         "SlideCommands": [
             {"JumpToSlide": 0},
             {"SetTextProperties.0": {
-                "FontHeightTwips": 420,
+                "FontHeightPoints": 21,
                 "FontFamily": "Liberation Sans",
                 "Bold": true,
                 "Italic": true
@@ -755,7 +759,9 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedTextFormattingAppliesToAllScripts
             aAttributes.begin(), aAttributes.end(), [&](const EECharAttrib& rAttribute) {
                 const auto* pHeight = dynamic_cast<const SvxFontHeightItem*>(rAttribute.pAttr);
                 return rAttribute.pAttr->Which() == nWhich && isFullRange(rAttribute)
-                       && pHeight && pHeight->GetHeight() == 420;
+                       && pHeight
+                       && pHeight->GetHeight()
+                              == static_cast<sal_uInt32>(convertPointToMm100(21.0));
             }));
     for (const sal_uInt16 nWhich : { EE_CHAR_FONTINFO, EE_CHAR_FONTINFO_CJK,
                                     EE_CHAR_FONTINFO_CTL })
@@ -782,10 +788,54 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedTextFormattingAppliesToAllScripts
                        && pPosture && pPosture->GetPosture() == ITALIC_NORMAL;
             }));
 
+    const auto assertUnoWholeRangeReadback = [&]() {
+        uno::Reference<text::XText> xText(pTextObject->getUnoShape(), uno::UNO_QUERY_THROW);
+        uno::Reference<text::XTextCursor> xCursor = xText->createTextCursor();
+        xCursor->gotoEnd(true);
+        uno::Reference<beans::XPropertySet> xRange(xCursor, uno::UNO_QUERY_THROW);
+        for (const OUString& rName : { u"CharHeight"_ustr,
+                                      u"CharHeightAsian"_ustr,
+                                      u"CharHeightComplex"_ustr })
+        {
+            float fHeight = 0.0f;
+            CPPUNIT_ASSERT(xRange->getPropertyValue(rName) >>= fHeight);
+            CPPUNIT_ASSERT_EQUAL_MESSAGE(rName.toUtf8().getStr(), 21.0f, fHeight);
+        }
+        for (const OUString& rName : { u"CharFontName"_ustr,
+                                      u"CharFontNameAsian"_ustr,
+                                      u"CharFontNameComplex"_ustr })
+        {
+            OUString aFamily;
+            CPPUNIT_ASSERT(xRange->getPropertyValue(rName) >>= aFamily);
+            CPPUNIT_ASSERT_EQUAL_MESSAGE(rName.toUtf8().getStr(),
+                                         u"Liberation Sans"_ustr, aFamily);
+        }
+        for (const OUString& rName : { u"CharWeight"_ustr,
+                                      u"CharWeightAsian"_ustr,
+                                      u"CharWeightComplex"_ustr })
+        {
+            float fWeight = 0.0f;
+            CPPUNIT_ASSERT(xRange->getPropertyValue(rName) >>= fWeight);
+            CPPUNIT_ASSERT_EQUAL_MESSAGE(rName.toUtf8().getStr(),
+                                         awt::FontWeight::BOLD, fWeight);
+        }
+        for (const OUString& rName : { u"CharPosture"_ustr,
+                                      u"CharPostureAsian"_ustr,
+                                      u"CharPostureComplex"_ustr })
+        {
+            awt::FontSlant eSlant = awt::FontSlant_NONE;
+            CPPUNIT_ASSERT(xRange->getPropertyValue(rName) >>= eSlant);
+            CPPUNIT_ASSERT_EQUAL_MESSAGE(rName.toUtf8().getStr(),
+                                         awt::FontSlant_ITALIC, eSlant);
+        }
+    };
+    assertUnoWholeRangeReadback();
+
     pUndoManager->Undo();
     CPPUNIT_ASSERT(aOriginal == *pTextObject->GetOutlinerParaObject());
     pUndoManager->Redo();
     CPPUNIT_ASSERT(aChanged == *pTextObject->GetOutlinerParaObject());
+    assertUnoWholeRangeReadback();
 }
 
 CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testPptxTextShadowRoundTrip)
@@ -3470,6 +3520,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedLayoutSelectsExistingMaster)
     SdPage* pPage = pDocument->GetDocShell()->GetViewShell()->GetActualPage();
     CPPUNIT_ASSERT(pDoc && pPage && pPage->TRG_HasMasterPage());
     SdPage* pOriginalMaster = static_cast<SdPage*>(&pPage->TRG_GetMasterPage());
+    const AutoLayout eOriginalLayout = pPage->GetAutoLayout();
 
     uno::Reference<drawing::XMasterPagesSupplier> xMasterPagesSupplier(
         mxComponent, uno::UNO_QUERY_THROW);
@@ -3482,6 +3533,7 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedLayoutSelectsExistingMaster)
     CPPUNIT_ASSERT(pAlternateMaster);
     pAlternateMaster->SetAutoLayout(AUTOLAYOUT_NONE, true, true);
     const OUString aAlternateLayoutName = pAlternateMaster->GetLayoutName();
+    const size_t nAlternateMasterObjectCount = pAlternateMaster->GetObjCount();
     const sal_uInt16 nMasterCount = pDoc->GetMasterSdPageCount(PageKind::Standard);
 
     static constexpr OUString aJson = uR"json(
@@ -3513,6 +3565,40 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedLayoutSelectsExistingMaster)
     CPPUNIT_ASSERT_EQUAL(AUTOLAYOUT_NONE, pPage->GetAutoLayout());
     CPPUNIT_ASSERT_EQUAL(aAlternateLayoutName, pPage->GetLayoutName());
     CPPUNIT_ASSERT(&pPage->TRG_GetMasterPage() == pAlternateMaster);
+    CPPUNIT_ASSERT_EQUAL(AUTOLAYOUT_NONE, pAlternateMaster->GetAutoLayout());
+    CPPUNIT_ASSERT_EQUAL(nAlternateMasterObjectCount, pAlternateMaster->GetObjCount());
+
+    // Imported PPTX master pages often carry AUTOLAYOUT_NONE internally even
+    // though the selected slide-layout part projects a concrete UNO
+    // SlideLayout. The explicit layout must therefore be applied to the slide
+    // without changing or cloning the selected master.
+    pUndoManager->Undo();
+    CPPUNIT_ASSERT_EQUAL(eOriginalLayout, pPage->GetAutoLayout());
+    CPPUNIT_ASSERT(&pPage->TRG_GetMasterPage() == pOriginalMaster);
+    static constexpr OUString aExplicitLayoutJson = uR"json(
+{
+    "Transforms": {
+        "SlideCommands": [
+            {"ChangeLayout": {"MasterIndex": 1, "Layout": 0}}
+        ]
+    }
+}
+)json"_ustr;
+    dispatchCommand(mxComponent, u".uno:TransformDocumentStructure"_ustr,
+                    { comphelper::makePropertyValue(u"DataJson"_ustr,
+                                                    aExplicitLayoutJson) });
+    CPPUNIT_ASSERT_EQUAL(static_cast<AutoLayout>(0), pPage->GetAutoLayout());
+    CPPUNIT_ASSERT_EQUAL(aAlternateLayoutName, pPage->GetLayoutName());
+    CPPUNIT_ASSERT(&pPage->TRG_GetMasterPage() == pAlternateMaster);
+    CPPUNIT_ASSERT_EQUAL(AUTOLAYOUT_NONE, pAlternateMaster->GetAutoLayout());
+    CPPUNIT_ASSERT_EQUAL(nAlternateMasterObjectCount, pAlternateMaster->GetObjCount());
+
+    pUndoManager->Undo();
+    CPPUNIT_ASSERT_EQUAL(eOriginalLayout, pPage->GetAutoLayout());
+    CPPUNIT_ASSERT(&pPage->TRG_GetMasterPage() == pOriginalMaster);
+    pUndoManager->Redo();
+    CPPUNIT_ASSERT_EQUAL(static_cast<AutoLayout>(0), pPage->GetAutoLayout());
+    CPPUNIT_ASSERT(&pPage->TRG_GetMasterPage() == pAlternateMaster);
 }
 
 CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testTypedLayoutUndoRestoresAnimationTree)
diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
index f2dabf1355a52817961386f8ac396a3276a67b03..0c8e175a986a57a63b993efb9276bbdbd0c1f435 100644
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -1488,11 +1488,14 @@ void handleChangeLayout(SlideCommandContext& rCtx, const std::string& rKey,
                 static_cast<sal_uInt16>(nSelectedMasterIndex), PageKind::Standard);
             if (!pSelectedMaster)
                 throw lang::IllegalArgumentException(u"Master layout not found"_ustr, nullptr, 0);
-            nLayoutId = pSelectedMaster->GetAutoLayout();
-            if (oExplicitLayout && *oExplicitLayout != static_cast<sal_Int32>(nLayoutId))
-                throw lang::IllegalArgumentException(
-                    u"Master layout identity changed; observe the document again"_ustr,
-                    nullptr, 0);
+            // A master page's internal AutoLayout is not the selected slide's
+            // UNO SlideLayout. Imported PPTX masters commonly use
+            // AUTOLAYOUT_NONE internally while their slide-layout projection
+            // is Title, Blank, or another concrete layout. Keep master
+            // identity and slide layout as two independent typed inputs.
+            nLayoutId = oExplicitLayout
+                            ? static_cast<AutoLayout>(*oExplicitLayout)
+                            : pSelectedMaster->GetAutoLayout();
         }
         else
         {
@@ -1641,6 +1644,12 @@ void handleSetTextRange(SlideCommandContext& rCtx, const std::string& rKey,
     if (!pObject->IsSdrTextObj())
         throw lang::IllegalArgumentException(u"Object has no editable text"_ustr, nullptr, 0);
     auto* pTextObject = static_cast<SdrTextObj*>(pObject);
+    // A live text-edit outliner is a separate model from the stored shape
+    // paragraphs. Commit the user's current edit before applying a whole-
+    // object AI format command, otherwise UNO observation keeps reading that
+    // stale view model even after the stored object changes.
+    if (rCtx.mpDrawView && rCtx.mpDrawView->GetTextEditObject())
+        rCtx.mpDrawView->SdrEndTextEdit();
     const OutlinerParaObject* pOriginal = pTextObject->GetOutlinerParaObject();
     if (!pOriginal)
         throw lang::IllegalArgumentException(u"Object has no text paragraphs"_ustr, nullptr, 0);
@@ -1890,7 +1899,7 @@ void handleSetTextProperties(SlideCommandContext& rCtx, const std::string& rKey,
         "Escapement",
         "EscapementHeight",
         "FontFamily",
-        "FontHeightTwips",
+        "FontHeightPoints",
         "Italic",
     };
     for (const auto& rEntry : rValue)
@@ -1905,7 +1914,7 @@ void handleSetTextProperties(SlideCommandContext& rCtx, const std::string& rKey,
     const auto oKerning = rValue.get_optional<sal_Int32>("Kerning");
     const auto oEscapement = rValue.get_optional<sal_Int32>("Escapement");
     const auto oEscapementHeight = rValue.get_optional<sal_Int32>("EscapementHeight");
-    const auto oFontHeightTwips = rValue.get_optional<sal_Int32>("FontHeightTwips");
+    const auto oFontHeightPoints = rValue.get_optional<double>("FontHeightPoints");
     const auto oItalic = rValue.get_optional<bool>("Italic");
     std::optional<OUString> oFontFamily;
     if (const auto oFamily = rValue.get_optional<std::string>("FontFamily"))
@@ -1919,14 +1928,14 @@ void handleSetTextProperties(SlideCommandContext& rCtx, const std::string& rKey,
         oFontFamily = std::move(aFamily);
     }
     if (!oBold && !oKerning && !oEscapement && !oEscapementHeight && !oFontFamily
-        && !oFontHeightTwips && !oItalic)
+        && !oFontHeightPoints && !oItalic)
         throw lang::IllegalArgumentException(u"No text properties supplied"_ustr, nullptr, 0);
     if (oEscapement.has_value() != oEscapementHeight.has_value())
         throw lang::IllegalArgumentException(
             u"Escapement and height must be supplied together"_ustr, nullptr, 0);
     if (oKerning && (*oKerning < -10000 || *oKerning > 10000))
         throw lang::IllegalArgumentException(u"Text kerning out of range"_ustr, nullptr, 0);
-    if (oFontHeightTwips && (*oFontHeightTwips < 20 || *oFontHeightTwips > 8000))
+    if (oFontHeightPoints && (*oFontHeightPoints < 1.0 || *oFontHeightPoints > 400.0))
         throw lang::IllegalArgumentException(u"Text font height out of range"_ustr, nullptr, 0);
     if (oEscapement
         && (*oEscapement < -100 || *oEscapement > 100 || *oEscapementHeight < 1
@@ -1941,12 +1950,13 @@ void handleSetTextProperties(SlideCommandContext& rCtx, const std::string& rKey,
     pOutliner->Init(pOriginal->GetOutlinerMode());
     pOutliner->SetText(*pOriginal);
     SfxItemSet aTextAttributes(pOutliner->GetEmptyItemSet());
-    if (oFontHeightTwips)
+    if (oFontHeightPoints)
     {
+        const sal_uInt32 nHeightMm100 = static_cast<sal_uInt32>(
+            convertPointToMm100(*oFontHeightPoints));
         for (const sal_uInt16 nWhich : { EE_CHAR_FONTHEIGHT, EE_CHAR_FONTHEIGHT_CJK,
                                         EE_CHAR_FONTHEIGHT_CTL })
-            aTextAttributes.Put(SvxFontHeightItem(
-                static_cast<sal_uInt32>(*oFontHeightTwips), 100, nWhich));
+            aTextAttributes.Put(SvxFontHeightItem(nHeightMm100, 100, nWhich));
     }
     if (oFontFamily)
     {
@@ -1991,6 +2001,10 @@ void handleSetTextProperties(SlideCommandContext& rCtx, const std::string& rKey,
     for (sal_Int32 nParagraph = 0; nParagraph < pOutliner->GetParagraphCount(); ++nParagraph)
         pOutliner->SetCharAttribs(nParagraph, aTextAttributes);
     pTextObject->SetOutlinerParaObject(pOutliner->CreateParaObject());
+    // Existing UNO text ranges keep a cached background outliner. Notify the
+    // shape after the complete internal mutation so browser-side observation
+    // reloads the new portions immediately instead of waiting for save/reopen.
+    pObject->BroadcastObjectChange();
     rCtx.touch(pPage);
 }
 
