diff --git a/oox/source/export/drawingml.cxx b/oox/source/export/drawingml.cxx
--- a/oox/source/export/drawingml.cxx
+++ b/oox/source/export/drawingml.cxx
@@ -3671,7 +3671,12 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
     sal_Int32 nParaLeftMargin = 0;
+    sal_Int32 nParaRightMargin = 0;
     sal_Int32 nParaFirstLineIndent = 0;

     if (GetProperty(rXPropSet, u"ParaLeftMargin"_ustr))
         mAny >>= nParaLeftMargin;
+    bool bDirectRightMargin = false;
+    if (GetPropertyAndState(rXPropSet, rXPropState, u"ParaRightMargin"_ustr, eState)
+        && (mAny >>= nParaRightMargin))
+        bDirectRightMargin = eState == beans::PropertyState_DIRECT_VALUE;
     if (GetProperty(rXPropSet, u"ParaFirstLineIndent"_ustr))
         mAny >>= nParaFirstLineIndent;
@@ -3707,5 +3711,7 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
         nParaLeftMargin = 0;
     if (nLeftMargin > nMaxMarginVal)
         nLeftMargin = 0;
+    const bool bWriteRightMargin = nParaRightMargin >= 0 && nParaRightMargin <= nMaxMarginVal
+        && (nParaRightMargin > 0 || bDirectRightMargin);

     bool bIsListStyle = (nElement >= XML_lvl1pPr && nElement <= XML_lvl9pPr);
@@ -3713,6 +3719,7 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
     if (nParaLeftMargin || bIsListStyle) // For Paragraph or List-Style elements
         mpFS->startElementNS( XML_a, nElement,
                            XML_marL, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaLeftMargin)), nParaLeftMargin > 0),
+                           XML_marR, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaRightMargin)), bWriteRightMargin),
                            XML_lvl, sax_fastparser::UseIf(OString::number(nOutLevel), nOutLevel > 0),
                            XML_indent, sax_fastparser::UseIf(OString::number((bForceZeroIndent && nParaFirstLineIndent == 0) ? 0 : oox::drawingml::convertHmmToEmu(nParaFirstLineIndent)), (bForceZeroIndent || nParaFirstLineIndent != 0)),
                            XML_algn, GetAlignment( nAlignment, mbPlaceholder ),
@@ -3721,7 +3729,8 @@ bool DrawingML::WriteParagraphProperties(const Reference<XTextContent>& rParag
                            XML_rtl, sax_fastparser::UseIf(ToPsz10(bRtl), bRtl));
     else
         mpFS->startElementNS( XML_a, nElement,
                            XML_marL, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nLeftMargin)), nLeftMargin > 0),
+                           XML_marR, sax_fastparser::UseIf(OString::number(oox::drawingml::convertHmmToEmu(nParaRightMargin)), bWriteRightMargin),
                            XML_lvl, sax_fastparser::UseIf(OString::number(nOutLevel), nOutLevel > 0),
                            XML_indent, sax_fastparser::UseIf(OString::number(!bForceZeroIndent ? oox::drawingml::convertHmmToEmu(nLineIndentation) : 0), (bForceZeroIndent || ( nLineIndentation != 0))),
                            XML_algn, GetAlignment( nAlignment, mbPlaceholder ),
diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -2977,6 +2977,6 @@ void handleSetParagraphProperties(SlideCommandContext& rCtx, const std::string&
     for (const auto& [pName, pProperty, nMinimum] : {
-             std::tuple("LeftMargin", u"ParaLeftMargin", sal_Int32(-100000)),
-             std::tuple("RightMargin", u"ParaRightMargin", sal_Int32(-100000)),
+             std::tuple("LeftMargin", u"ParaLeftMargin", sal_Int32(0)),
+             std::tuple("RightMargin", u"ParaRightMargin", sal_Int32(0)),
              std::tuple("FirstLineIndent", u"ParaFirstLineIndent", sal_Int32(-100000)),
              std::tuple("TopMargin", u"ParaTopMargin", sal_Int32(0)),
              std::tuple("BottomMargin", u"ParaBottomMargin", sal_Int32(0)),
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -1294,10 +1294,18 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParag
         xParagraph = paragraphProperties();
         sal_Int32 nValue = 0;
         sal_Int16 nWritingMode = 0;
-        xParagraph->getPropertyValue(u"ParaLeftMargin"_ustr) >>= nValue;
-        CPPUNIT_ASSERT_EQUAL(sal_Int32(1200), nValue);
+        // Import may represent marL/indent in NumberingRules rather than
+        // ParaLeftMargin/ParaFirstLineIndent. The saved OOXML is asserted below.
+        if (!bAfterReload)
+        {
+            xParagraph->getPropertyValue(u"ParaLeftMargin"_ustr) >>= nValue;
+            CPPUNIT_ASSERT_EQUAL(sal_Int32(1200), nValue);
+        }
         xParagraph->getPropertyValue(u"ParaRightMargin"_ustr) >>= nValue;
         CPPUNIT_ASSERT_EQUAL(sal_Int32(300), nValue);
-        xParagraph->getPropertyValue(u"ParaFirstLineIndent"_ustr) >>= nValue;
-        CPPUNIT_ASSERT_EQUAL(sal_Int32(-200), nValue);
+        if (!bAfterReload)
+        {
+            xParagraph->getPropertyValue(u"ParaFirstLineIndent"_ustr) >>= nValue;
+            CPPUNIT_ASSERT_EQUAL(sal_Int32(-200), nValue);
+        }
         xParagraph->getPropertyValue(u"ParaTopMargin"_ustr) >>= nValue;
@@ -1328,6 +1336,10 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParag
     pUndoManager->Redo();
     assertChanged();
     saveAndReload(TestFilter::PPTX);
+    // Inspect the authored package before interpreting UNO's reopened
+    // paragraph properties; this separates export loss from import remapping.
+    xmlDocUniquePtr pSlideXml = parseExport(u"ppt/slides/slide1.xml"_ustr);
+    assertXPath(pSlideXml, "//a:pPr[@marL='432000' and @marR='108000']", 1);
     pImpressDocument = dynamic_cast<SdXImpressDocument*>(mxComponent.get());
     CPPUNIT_ASSERT(pImpressDocument);
     pDoc = pImpressDocument->GetDoc();
