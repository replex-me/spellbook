diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -46,3 +46,3 @@
 #include <com/sun/star/drawing/LineStyle.hpp>
-#include <com/sun/star/drawing/PointSequence.hpp>
+#include <com/sun/star/drawing/PolyPolygonBezierCoords.hpp>
 #include <com/sun/star/drawing/FillStyle.hpp>
@@ -1272,11 +1272,13 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphProperties)
         {
             // PPTX persists marker geometry, not LibreOffice palette names.
-            drawing::PointSequence aStartMarker;
-            drawing::PointSequence aEndMarker;
+            drawing::PolyPolygonBezierCoords aStartMarker;
+            drawing::PolyPolygonBezierCoords aEndMarker;
             CPPUNIT_ASSERT(xShapeProperties->getPropertyValue(u"LineStart"_ustr)
                            >>= aStartMarker);
             CPPUNIT_ASSERT(xShapeProperties->getPropertyValue(u"LineEnd"_ustr)
                            >>= aEndMarker);
-            CPPUNIT_ASSERT(aStartMarker.getLength() > 2);
-            CPPUNIT_ASSERT(aEndMarker.getLength() > 2);
+            CPPUNIT_ASSERT(aStartMarker.Coordinates.getLength() > 0);
+            CPPUNIT_ASSERT(aEndMarker.Coordinates.getLength() > 0);
+            CPPUNIT_ASSERT(aStartMarker.Coordinates[0].getLength() > 2);
+            CPPUNIT_ASSERT(aEndMarker.Coordinates[0].getLength() > 2);
         }
