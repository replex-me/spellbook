diff --git a/include/oox/drawingml/shape.hxx b/include/oox/drawingml/shape.hxx
index f5cd2e29a8da5d4f2b7d87933e3fffdb649ec6f4..bea995de818c3fbc7533945f27353d44c0a47e17 100644
--- a/include/oox/drawingml/shape.hxx
+++ b/include/oox/drawingml/shape.hxx
@@ -189,6 +189,8 @@ public:
     void                            setHidden( bool bHidden ) { mbHidden = bHidden; }
     void                            setHiddenMasterShape( bool bHiddenMasterShape ) { mbHiddenMasterShape = bHiddenMasterShape; }
     void                            setLocked( bool bLocked ) { mbLocked = bLocked; }
+    void                            setMoveLocked( bool bLocked ) { mbMoveLocked = bLocked; }
+    void                            setSizeLocked( bool bLocked ) { mbSizeLocked = bLocked; }
     void                            setSubType( sal_Int32 nSubType ) { mnSubType = nSubType; }
     sal_Int32                       getSubType() const { return mnSubType; }
     void                            setSubTypeIndex( sal_Int32 nSubTypeIndex ) { moSubTypeIndex = nSubTypeIndex; }
@@ -435,6 +437,8 @@ private:
                                                          // we need separate flag because we don't want
                                                          // to propagate it when applying reference shape
     bool                            mbLocked;
+    bool                            mbMoveLocked;
+    bool                            mbSizeLocked;
     bool mbWPGChild; // Is this shape a child of a WPG shape?
     bool mbLockedCanvas; ///< Is this shape part of a locked canvas?
     bool mbWordprocessingCanvas; ///< Is this shape part of a wordprocessing canvas?
diff --git a/include/oox/export/shapes.hxx b/include/oox/export/shapes.hxx
index 8a0b10c445b67cb730c6b33f992dc5c3227a8e58..c04fc523abfb68d395c89160997b3ec39b6cef9e 100644
--- a/include/oox/export/shapes.hxx
+++ b/include/oox/export/shapes.hxx
@@ -91,6 +91,8 @@ private:
     std::shared_ptr<URLTransformer> mpURLTransformer;
 
     css::awt::Size MapSize( const css::awt::Size& ) const;
+    void WriteShapeLocks(const css::uno::Reference<css::drawing::XShape>& xShape,
+                         sal_Int32 nLockToken);
 
     ShapeHashMap maShapeMap;
     ShapeHashMap* mpShapeMap;
diff --git a/oox/source/drawingml/connectorshapecontext.cxx b/oox/source/drawingml/connectorshapecontext.cxx
index 5800a52cfb4931a17b96d2d219c07b2044c47900..59a6ba20aed5e24974c73dfa7b6824de466e0674 100644
--- a/oox/source/drawingml/connectorshapecontext.cxx
+++ b/oox/source/drawingml/connectorshapecontext.cxx
@@ -60,6 +60,10 @@ ConnectorShapePropertiesContext::onCreateContext(sal_Int32 aElementToken,
             mpConnectorShapePtr->setId(rAttribs.getStringDefaulted(XML_id));
             mpConnectorShapePtr->setName(rAttribs.getStringDefaulted(XML_name));
             break;
+        case XML_cxnSpLocks:
+            mpConnectorShapePtr->setMoveLocked(rAttribs.getBool(XML_noMove, false));
+            mpConnectorShapePtr->setSizeLocked(rAttribs.getBool(XML_noResize, false));
+            break;
         case XML_stCxn:
         {
             ConnectorShapeProperties aConnectorShapeProps;
diff --git a/oox/source/drawingml/graphicshapecontext.cxx b/oox/source/drawingml/graphicshapecontext.cxx
index 0a143623db18793487b2a63f078b2cfee2b0b580..a69cf420dc84fb24ce0da52ee1c837cf7373178b 100644
--- a/oox/source/drawingml/graphicshapecontext.cxx
+++ b/oox/source/drawingml/graphicshapecontext.cxx
@@ -158,6 +158,10 @@ ContextHandlerRef GraphicalObjectFrameContext::onCreateContext( sal_Int32 aEleme
         maName = rAttribs.getStringDefaulted(XML_name);
         break;
     }
+    case XML_graphicFrameLocks:
+        mpShapePtr->setMoveLocked(rAttribs.getBool(XML_noMove, false));
+        mpShapePtr->setSizeLocked(rAttribs.getBool(XML_noResize, false));
+        break;
     case XML_xfrm:                  // CT_Transform2D
         return new Transform2DContext( *this, rAttribs, *mpShapePtr );
     case XML_graphic:               // CT_GraphicalObject
diff --git a/oox/source/drawingml/shape.cxx b/oox/source/drawingml/shape.cxx
index ecd3bc9ea27fdfe17319d61c277e2f648d5337de..449d1198165462b8efeeb9e2f0c8104718692c57 100644
--- a/oox/source/drawingml/shape.cxx
+++ b/oox/source/drawingml/shape.cxx
@@ -165,6 +165,8 @@ Shape::Shape()
 , mbHidden( false )
 , mbHiddenMasterShape( false )
 , mbLocked( false )
+, mbMoveLocked( false )
+, mbSizeLocked( false )
 , mbWPGChild(false)
 , mbLockedCanvas( false )
 , mbWordprocessingCanvas(false)
@@ -198,6 +200,8 @@ Shape::Shape( const OUString& rServiceName, bool bDefaultHeight )
 , mbHidden( false )
 , mbHiddenMasterShape( false )
 , mbLocked( false )
+, mbMoveLocked( false )
+, mbSizeLocked( false )
 , mbWPGChild(false)
 , mbLockedCanvas( false )
 , mbWordprocessingCanvas(false)
@@ -242,6 +246,8 @@ Shape::Shape( const ShapePtr& pSourceShape )
 , mbHidden( pSourceShape->mbHidden )
 , mbHiddenMasterShape( pSourceShape->mbHiddenMasterShape )
 , mbLocked( pSourceShape->mbLocked )
+, mbMoveLocked( pSourceShape->mbMoveLocked )
+, mbSizeLocked( pSourceShape->mbSizeLocked )
 , mbWPGChild( pSourceShape->mbWPGChild )
 , mbLockedCanvas( pSourceShape->mbLockedCanvas )
 , mbWordprocessingCanvas(pSourceShape->mbWordprocessingCanvas)
@@ -617,6 +623,8 @@ void Shape::applyShapeReference(const Shape& rReferencedShape, ReferencedShapeTe
     mbFlipV = rReferencedShape.mbFlipV;
     mbHidden = rReferencedShape.mbHidden;
     mbLocked = rReferencedShape.mbLocked;
+    mbMoveLocked = rReferencedShape.mbMoveLocked;
+    mbSizeLocked = rReferencedShape.mbSizeLocked;
 }
 
 namespace {
@@ -1652,11 +1660,10 @@ Reference< XShape > const & Shape::createAndInsert(
             xSet->setPropertyValue( u"Printable"_ustr, Any( false ) );
         }
 
-        if (mbLocked)
-        {
+        if (mbLocked || mbMoveLocked)
             xSet->setPropertyValue(u"MoveProtect"_ustr, Any(true));
+        if (mbLocked || mbSizeLocked)
             xSet->setPropertyValue(u"SizeProtect"_ustr, Any(true));
-        }
 
         ActionLockGuard const alg(mxShape);
 
diff --git a/oox/source/drawingml/shapecontext.cxx b/oox/source/drawingml/shapecontext.cxx
index adedaf970913197a8e132f369c2c9bfe3bfac699..79e42ed01207a0bcd267ffeeb852e0122d8abb80 100644
--- a/oox/source/drawingml/shapecontext.cxx
+++ b/oox/source/drawingml/shapecontext.cxx
@@ -117,6 +117,8 @@ ContextHandlerRef ShapeContext::onCreateContext( sal_Int32 aElementToken, const
     case XML_cNvSpPr:
         break;
     case XML_spLocks:
+        mpShapePtr->setMoveLocked(rAttribs.getBool(XML_noMove, false));
+        mpShapePtr->setSizeLocked(rAttribs.getBool(XML_noResize, false));
         break;
     case XML_bodyPr:
         if (!mpShapePtr->getTextBody())
@@ -128,6 +130,8 @@ ContextHandlerRef ShapeContext::onCreateContext( sal_Int32 aElementToken, const
         break;
     case XML_nvPicPr:
     case XML_picLocks:
+        mpShapePtr->setMoveLocked(rAttribs.getBool(XML_noMove, false));
+        mpShapePtr->setSizeLocked(rAttribs.getBool(XML_noResize, false));
         break;
     case XML_relIds:
         break;
diff --git a/oox/source/drawingml/shapegroupcontext.cxx b/oox/source/drawingml/shapegroupcontext.cxx
index 3963489ceed54ce76462d6465c89e053eb87d7ad..5be398b6a05542be74314fc3d9b8be73f8338502 100644
--- a/oox/source/drawingml/shapegroupcontext.cxx
+++ b/oox/source/drawingml/shapegroupcontext.cxx
@@ -125,6 +125,8 @@ ContextHandlerRef ShapeGroupContext::onCreateContext( sal_Int32 aElementToken, c
     case XML_cNvGrpSpPr:
         break;
     case XML_grpSpLocks:
+        mpGroupShapePtr->setMoveLocked(rAttribs.getBool(XML_noMove, false));
+        mpGroupShapePtr->setSizeLocked(rAttribs.getBool(XML_noResize, false));
         break;
     default:
         SAL_WARN("oox", "ShapeGroupContext::onCreateContext: unhandled element: " << getBaseToken(aElementToken));
diff --git a/oox/source/export/shapes.cxx b/oox/source/export/shapes.cxx
index f025ca99fbebdc133570828c9d548821346d3e6d..a55590646b9823a5c0541257c4bbd16f58f2dd29 100644
--- a/oox/source/export/shapes.cxx
+++ b/oox/source/export/shapes.cxx
@@ -354,6 +354,29 @@ awt::Size ShapeExport::MapSize( const awt::Size& rSize ) const
     return awt::Size( aRetSize.Width(), aRetSize.Height() );
 }
 
+void ShapeExport::WriteShapeLocks(const Reference<XShape>& xShape, sal_Int32 nLockToken)
+{
+    if (GetDocumentType() != DOCUMENT_PPTX)
+        return;
+
+    Reference<XPropertySet> xProperties(xShape, UNO_QUERY);
+    if (!xProperties.is())
+        return;
+
+    bool bMoveProtected = false;
+    if (GetProperty(xProperties, u"MoveProtect"_ustr))
+        mAny >>= bMoveProtected;
+    bool bSizeProtected = false;
+    if (GetProperty(xProperties, u"SizeProtect"_ustr))
+        mAny >>= bSizeProtected;
+    if (!bMoveProtected && !bSizeProtected)
+        return;
+
+    GetFS()->singleElementNS(XML_a, nLockToken,
+                             XML_noMove, sax_fastparser::UseIf("1", bMoveProtected),
+                             XML_noResize, sax_fastparser::UseIf("1", bSizeProtected));
+}
+
 static bool IsNonEmptySimpleText(const Reference<cpo::uno::XInterface>& xIface)
 {
     if (Reference<XSimpleText> xText{ xIface, UNO_QUERY })
@@ -452,7 +475,9 @@ ShapeExport& ShapeExport::WritePolyPolygonShape( const Reference< XShape >& xSha
         AddExtLst(pFS, xProps);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
     }
-    pFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr);
+    pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr);
+    WriteShapeLocks(xShape, XML_spLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
     if (GetDocumentType() != DOCUMENT_DOCX || mbUserShapes)
     {
         WriteNonVisualProperties( xShape );
@@ -518,7 +543,9 @@ ShapeExport& ShapeExport::WriteGroupShape(const uno::Reference<drawing::XShape>&
                             XML_hidden, sax_fastparser::UseIf("1", !isVisible));
         AddExtLst(pFS, xShapeProps);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-        pFS->singleElementNS(mnXmlNamespace, XML_cNvGrpSpPr);
+        pFS->startElementNS(mnXmlNamespace, XML_cNvGrpSpPr);
+        WriteShapeLocks(xShape, XML_grpSpLocks);
+        pFS->endElementNS(mnXmlNamespace, XML_cNvGrpSpPr);
         WriteNonVisualProperties(xShape );
         pFS->endElementNS(mnXmlNamespace, XML_nvGrpSpPr);
     }
@@ -1065,7 +1092,9 @@ ShapeExport& ShapeExport::WriteCustomShape( const Reference< XShape >& xShape )
         }
         AddExtLst(pFS, rXPropSet);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-        pFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr);
+        pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr);
+        WriteShapeLocks(xShape, XML_spLocks);
+        pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
         WriteNonVisualProperties( xShape );
         pFS->endElementNS( mnXmlNamespace, XML_nvSpPr );
     }
@@ -1332,7 +1361,9 @@ ShapeExport& ShapeExport::WriteEllipseShape( const Reference< XShape >& xShape )
                 XML_name, GetShapeName(xShape));
         AddExtLst(pFS, xProps);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-        pFS->singleElementNS( mnXmlNamespace, XML_cNvSpPr );
+        pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr);
+        WriteShapeLocks(xShape, XML_spLocks);
+        pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
         WriteNonVisualProperties( xShape );
         pFS->endElementNS( mnXmlNamespace, XML_nvSpPr );
     }
@@ -1553,9 +1584,9 @@ void ShapeExport::WriteGraphicObjectShapePart( const Reference< XShape >& xShape
     AddExtLst(pFS, xShapeProps);
     pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
 
-    pFS->singleElementNS(mnXmlNamespace, XML_cNvPicPr
-                         // OOXTODO: XML_preferRelativeSize
-                        );
+    pFS->startElementNS(mnXmlNamespace, XML_cNvPicPr);
+    WriteShapeLocks(xShape, XML_picLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvPicPr);
     if (bHasMediaURL)
         WriteMediaNonVisualProperties(xShape);
     else
@@ -2033,6 +2064,7 @@ ShapeExport& ShapeExport::WriteConnectorShape( const Reference< XShape >& xShape
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
         // non visual connector shape drawing properties
         pFS->startElementNS(mnXmlNamespace, XML_cNvCxnSpPr);
+        WriteShapeLocks(xShape, XML_cxnSpLocks);
 
         if (GetShapeID(rXShapeA) == -1)
             GetNewShapeID(rXShapeA);
@@ -2098,7 +2130,9 @@ ShapeExport& ShapeExport::WriteLineShape( const Reference< XShape >& xShape )
         AddExtLst(pFS, xShapeProps);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
     }
-    pFS->singleElementNS( mnXmlNamespace, XML_cNvSpPr );
+    pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr);
+    WriteShapeLocks(xShape, XML_spLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
     if (GetDocumentType() != DOCUMENT_DOCX || mbUserShapes)
     {
         WriteNonVisualProperties( xShape );
@@ -2178,7 +2212,9 @@ ShapeExport& ShapeExport::WriteRectangleShape( const Reference< XShape >& xShape
                           XML_name, GetShapeName(xShape));
     AddExtLst(pFS, xShapeProps);
     pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-    pFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr);
+    pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr);
+    WriteShapeLocks(xShape, XML_spLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
     WriteNonVisualProperties( xShape );
     pFS->endElementNS( mnXmlNamespace, XML_nvSpPr );
 
@@ -2714,7 +2750,9 @@ ShapeExport& ShapeExport::WriteTableShape( const Reference< XShape >& xShape )
     AddExtLst(pFS, xShapeProps);
     pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
 
-    pFS->singleElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
+    pFS->startElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
+    WriteShapeLocks(xShape, XML_graphicFrameLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
 
     if( GetDocumentType() == DOCUMENT_PPTX )
         pFS->singleElementNS(mnXmlNamespace, XML_nvPr);
@@ -2758,7 +2796,9 @@ ShapeExport& ShapeExport::WriteTextShape( const Reference< XShape >& xShape )
         AddExtLst(pFS, xShapeProps);
         pFS->endElementNS(mnXmlNamespace, XML_cNvPr);
     }
-    pFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+    pFS->startElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+    WriteShapeLocks(xShape, XML_spLocks);
+    pFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
     if (GetDocumentType() != DOCUMENT_DOCX || mbUserShapes)
     {
         WriteNonVisualProperties( xShape );
@@ -2808,7 +2848,9 @@ void ShapeExport::WriteMathShape(Reference<XShape> const& xShape)
     mpFS->startElementNS(mnXmlNamespace, XML_cNvPr, XML_id, cNvPr_id, XML_name, shapeName);
     AddExtLst(mpFS, xPropSet);
     mpFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-    mpFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+    mpFS->startElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+    WriteShapeLocks(xShape, XML_spLocks);
+    mpFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
     mpFS->singleElementNS(mnXmlNamespace, XML_nvPr);
     mpFS->endElementNS(mnXmlNamespace, XML_nvSpPr);
     mpFS->startElementNS(mnXmlNamespace, XML_spPr);
@@ -2846,7 +2888,9 @@ void ShapeExport::WriteMathShape(Reference<XShape> const& xShape)
             mpFS->startElementNS(mnXmlNamespace, XML_cNvPr, XML_id, cNvPr_id, XML_name, shapeName);
             AddExtLst(mpFS, xPropSet);
             mpFS->endElementNS(mnXmlNamespace, XML_cNvPr);
-            mpFS->singleElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+            mpFS->startElementNS(mnXmlNamespace, XML_cNvSpPr, XML_txBox, "1");
+            WriteShapeLocks(xShape, XML_spLocks);
+            mpFS->endElementNS(mnXmlNamespace, XML_cNvSpPr);
             mpFS->singleElementNS(mnXmlNamespace, XML_nvPr);
             mpFS->endElementNS(mnXmlNamespace, XML_nvSpPr);
             mpFS->startElementNS(mnXmlNamespace, XML_spPr);
@@ -3070,7 +3114,9 @@ ShapeExport& ShapeExport::WriteOLE2Shape( const Reference< XShape >& xShape )
     AddExtLst(mpFS, xPropSet);
     mpFS->endElementNS(mnXmlNamespace, XML_cNvPr);
 
-    mpFS->singleElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
+    mpFS->startElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
+    WriteShapeLocks(xShape, XML_graphicFrameLocks);
+    mpFS->endElementNS(mnXmlNamespace, XML_cNvGraphicFramePr);
 
     if (GetDocumentType() == DOCUMENT_PPTX)
         mpFS->singleElementNS(mnXmlNamespace, XML_nvPr);
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
index 40ea6702e0c310d9ac9db9a240546a68495cacae..23ca19ebd1833bacee7a2acb886f0e394ea4a6f7 100644
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -22,6 +22,7 @@
 #include <com/sun/star/beans/XPropertyContainer.hpp>
 #include <com/sun/star/beans/PropertyAttribute.hpp>
 #include <com/sun/star/container/XEnumerationAccess.hpp>
+#include <com/sun/star/container/XNamed.hpp>
 #include <com/sun/star/document/XDocumentPropertiesSupplier.hpp>
 #include <com/sun/star/document/XDocumentProperties.hpp>
 #include <com/sun/star/frame/Desktop.hpp>
@@ -744,6 +745,75 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testPptxTextShadowRoundTrip)
     CPPUNIT_ASSERT(bShadowed);
 }
 
+CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testPptxObjectLocksRoundTrip)
+{
+    createSdImpressDoc();
+    uno::Reference<drawing::XDrawPagesSupplier> xDrawPagesSupplier(mxComponent,
+                                                                   uno::UNO_QUERY_THROW);
+    uno::Reference<drawing::XDrawPage> xDrawPage(
+        xDrawPagesSupplier->getDrawPages()->getByIndex(0), uno::UNO_QUERY_THROW);
+    uno::Reference<drawing::XShapes> xShapes(xDrawPage, uno::UNO_QUERY_THROW);
+    uno::Reference<lang::XMultiServiceFactory> xFactory(mxComponent, uno::UNO_QUERY_THROW);
+
+    auto addShape = [&](const OUString& rService, const OUString& rName,
+                        const awt::Point& rPosition) {
+        uno::Reference<drawing::XShape> xShape(xFactory->createInstance(rService),
+                                               uno::UNO_QUERY_THROW);
+        xShape->setPosition(rPosition);
+        xShape->setSize(awt::Size(5000, 3000));
+        uno::Reference<container::XNamed>(xShape, uno::UNO_QUERY_THROW)->setName(rName);
+        xShapes->add(xShape);
+        return xShape;
+    };
+
+    uno::Reference<drawing::XShape> xMoveLocked = addShape(
+        u"com.sun.star.drawing.TextShape"_ustr, u"Move locked"_ustr, awt::Point(1000, 1000));
+    uno::Reference<beans::XPropertySet> xMoveProperties(xMoveLocked, uno::UNO_QUERY_THROW);
+    xMoveProperties->setPropertyValue(u"MoveProtect"_ustr, cpo::uno::Any(true));
+    xMoveProperties->setPropertyValue(u"SizeProtect"_ustr, cpo::uno::Any(false));
+
+    uno::Reference<drawing::XShape> xSizeLocked = addShape(
+        u"com.sun.star.drawing.RectangleShape"_ustr, u"Size locked"_ustr,
+        awt::Point(7000, 1000));
+    uno::Reference<beans::XPropertySet> xSizeProperties(xSizeLocked, uno::UNO_QUERY_THROW);
+    xSizeProperties->setPropertyValue(u"MoveProtect"_ustr, cpo::uno::Any(false));
+    xSizeProperties->setPropertyValue(u"SizeProtect"_ustr, cpo::uno::Any(true));
+
+    saveAndReload(TestFilter::PPTX);
+    xmlDocUniquePtr pXmlDoc = parseExport(u"ppt/slides/slide1.xml"_ustr);
+    assertXPath(pXmlDoc, "//a:spLocks[@noMove='1' and not(@noResize)]", 1);
+    assertXPath(pXmlDoc, "//a:spLocks[@noResize='1' and not(@noMove)]", 1);
+
+    xDrawPagesSupplier.set(mxComponent, uno::UNO_QUERY_THROW);
+    xDrawPage.set(xDrawPagesSupplier->getDrawPages()->getByIndex(0), uno::UNO_QUERY_THROW);
+    auto findShape = [&](const OUString& rName) {
+        for (sal_Int32 nIndex = 0; nIndex < xDrawPage->getCount(); ++nIndex)
+        {
+            uno::Reference<drawing::XShape> xShape(xDrawPage->getByIndex(nIndex),
+                                                   uno::UNO_QUERY_THROW);
+            uno::Reference<container::XNamed> xNamed(xShape, uno::UNO_QUERY);
+            if (xNamed.is() && xNamed->getName() == rName)
+                return xShape;
+        }
+        return uno::Reference<drawing::XShape>();
+    };
+    auto lockState = [](const uno::Reference<drawing::XShape>& xShape,
+                        const OUString& rPropertyName) {
+        CPPUNIT_ASSERT(xShape.is());
+        bool bValue = false;
+        uno::Reference<beans::XPropertySet>(xShape, uno::UNO_QUERY_THROW)
+            ->getPropertyValue(rPropertyName) >>= bValue;
+        return bValue;
+    };
+
+    xMoveLocked = findShape(u"Move locked"_ustr);
+    CPPUNIT_ASSERT(lockState(xMoveLocked, u"MoveProtect"_ustr));
+    CPPUNIT_ASSERT(!lockState(xMoveLocked, u"SizeProtect"_ustr));
+    xSizeLocked = findShape(u"Size locked"_ustr);
+    CPPUNIT_ASSERT(!lockState(xSizeLocked, u"MoveProtect"_ustr));
+    CPPUNIT_ASSERT(lockState(xSizeLocked, u"SizeProtect"_ustr));
+}
+
 CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureGraphicCropUndoRedo)
 {
     createSdImpressDoc("pptx/crop-position.pptx");
