diff --git a/oox/source/drawingml/shape.cxx b/oox/source/drawingml/shape.cxx
index 449d11981..4c9882e5b 100644
--- a/oox/source/drawingml/shape.cxx
+++ b/oox/source/drawingml/shape.cxx
@@ -600,6 +600,12 @@ void Shape::applyShapeReference(const Shape& rReferencedShape, ReferencedShapeTe
 {
     SAL_INFO("oox.drawingml", "Shape::applyShapeReference: apply '" << rReferencedShape.msId << "' to '" << msId << "'");

+    // A PPTX placeholder stores its own non-visual properties (for example
+    // hlinkClick) before its <p:ph> element is parsed.  The referenced master
+    // or layout shape supplies inherited defaults, but must not replace those
+    // already parsed local values.
+    const PropertyMap aLocalShapeProperties(maShapeProperties);
+
     if ( !rReferencedShape.mpTextBody || eText == ReferencedShapeText::Nothing )
         mpTextBody.reset();
     else if ( eText == ReferencedShapeText::All )
@@ -610,6 +616,7 @@ void Shape::applyShapeReference(const Shape& rReferencedShape, ReferencedShapeTe
         // altogether costs the inherited properties, which a placeholder needs to be laid out.
         mpTextBody = std::make_shared<TextBody>( rReferencedShape.mpTextBody );
     maShapeProperties = rReferencedShape.maShapeProperties;
+    maShapeProperties.assignAll(aLocalShapeProperties);
     mpShapeRefLinePropPtr = std::make_shared<LineProperties>( rReferencedShape.getActualLineProperties(nullptr) );
     mpShapeRefFillPropPtr = std::make_shared<FillProperties>( rReferencedShape.getActualFillProperties(nullptr, nullptr) );
     mpCustomShapePropertiesPtr = std::make_shared<CustomShapeProperties>( *rReferencedShape.mpCustomShapePropertiesPtr );
diff --git a/oox/source/ppt/pptgraphicshapecontext.cxx b/oox/source/ppt/pptgraphicshapecontext.cxx
index aba9474f1..59c370e21 100644
--- a/oox/source/ppt/pptgraphicshapecontext.cxx
+++ b/oox/source/ppt/pptgraphicshapecontext.cxx
@@ -156,8 +156,6 @@ ContextHandlerRef PPTGraphicShapeContext::onCreateContext( sal_Int32 aElementTok
                         case XML_pic :
                             eText = ReferencedShapeText::Nothing;
                     }
-                    pPlaceholder->getShapeProperties().setAnyProperty(
-                        PROP_URL, mpShapePtr->getShapeProperties().getProperty(PROP_URL));
                     mpShapePtr->applyShapeReference( *pPlaceholder, eText );
                     PPTShape* pPPTShape = dynamic_cast< PPTShape* >( pPlaceholder.get() );
                     if ( pPPTShape )
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
index 3de95a5a8..f540cb28a 100644
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -644,6 +644,8 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureObjectInteractionUndo
     SdPage* pPage = pDoc->GetSdPage(0, PageKind::Standard);
     CPPUNIT_ASSERT(pPage && pPage->GetObjCount() > 0);
     SdrObject* pObject = pPage->GetObj(0);
+    const PresObjKind eTargetKind = pPage->GetPresObjKind(pObject);
+    CPPUNIT_ASSERT_EQUAL(PresObjKind::Title, eTargetKind);

     SdAnimationInfo* pOldInfo = SdDrawDocument::GetShapeUserData(*pObject, true);
     pOldInfo->mbDimHide = true;
@@ -700,11 +702,22 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureObjectInteractionUndo
     assertAdjacentState();

     saveAndReload(TestFilter::PPTX);
+    xmlDocUniquePtr pSlideXml = parseExport(u"ppt/slides/slide1.xml"_ustr);
+    assertXPath(pSlideXml, "//p:sp/p:nvSpPr/p:cNvPr/a:hlinkClick", 1);
+    xmlDocUniquePtr pSlideRels
+        = parseExport(u"ppt/slides/_rels/slide1.xml.rels"_ustr);
+    static constexpr OString aHyperlinkXPath(
+        "/rels:Relationships/rels:Relationship[@Type='http://"
+        "schemas.openxmlformats.org/officeDocument/2006/relationships/hyperlink']"_ostr);
+    assertXPath(pSlideRels, aHyperlinkXPath, "Target",
+                u"https://example.invalid/presentation"_ustr);
+    assertXPath(pSlideRels, aHyperlinkXPath, "TargetMode", u"External"_ustr);
     pImpressDocument = dynamic_cast<SdXImpressDocument*>(mxComponent.get());
     CPPUNIT_ASSERT(pImpressDocument);
     pPage = pImpressDocument->GetDoc()->GetSdPage(0, PageKind::Standard);
     CPPUNIT_ASSERT(pPage && pPage->GetObjCount() > 0);
-    pObject = pPage->GetObj(0);
+    pObject = pPage->GetPresObj(eTargetKind);
+    CPPUNIT_ASSERT(pObject);
     std::tie(eAction, aBookmark) = getInteraction();
     CPPUNIT_ASSERT_EQUAL(presentation::ClickAction_DOCUMENT, eAction);
     CPPUNIT_ASSERT_EQUAL(u"https://example.invalid/presentation"_ustr, aBookmark);
