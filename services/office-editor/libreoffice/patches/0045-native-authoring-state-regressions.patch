diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
index 20fb12f0a..043652164 100644
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -2970,5 +2970,5 @@ void handleSetParagraphProperties(SlideCommandContext& rCtx, const std::string&
     static const std::set<std::string> aAllowedKeys = {
-        "Paragraph", "LastLineAlignment", "LeftMargin", "RightMargin",
+        "Paragraph", "LeftMargin", "RightMargin",
         "FirstLineIndent", "TopMargin", "BottomMargin", "Direction",
         "ListType", "Level", "Prefix", "Suffix", "StartWith", "BulletCharacter",
     };
@@ -3006,19 +3006,3 @@ void handleSetParagraphProperties(SlideCommandContext& rCtx, const std::string&
     std::vector<std::pair<OUString, cpo::uno::Any>> aValues;
-    if (const auto oAlignment = rValue.get_optional<std::string>("LastLineAlignment"))
-    {
-        static const std::map<std::string, style::ParagraphAdjust> aAlignmentValues = {
-            { "left", style::ParagraphAdjust_LEFT },
-            { "center", style::ParagraphAdjust_CENTER },
-            { "right", style::ParagraphAdjust_RIGHT },
-            { "justify", style::ParagraphAdjust_BLOCK },
-        };
-        const auto it = aAlignmentValues.find(*oAlignment);
-        if (it == aAlignmentValues.end())
-            throw lang::IllegalArgumentException(u"Paragraph alignment is invalid"_ustr,
-                                                  nullptr, 0);
-        aValues.emplace_back(u"ParaLastLineAdjust"_ustr, cpo::uno::Any(it->second));
-        aValues.emplace_back(u"ParaAdjust"_ustr,
-                             cpo::uno::Any(style::ParagraphAdjust_BLOCK));
-    }
     for (const auto& [pName, pProperty, nMinimum] : {
              std::tuple("LeftMargin", u"ParaLeftMargin", sal_Int32(-100000)),
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -1200,10 +1200,8 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
     auto xParagraph = paragraphProperties();
-    style::ParagraphAdjust eOldLastLine = style::ParagraphAdjust_LEFT;
     sal_Int32 nOldLeftMargin = 0;
     sal_Int32 nOldRightMargin = 0;
     sal_Int32 nOldFirstLineIndent = 0;
     sal_Int32 nOldTopMargin = 0;
     sal_Int32 nOldBottomMargin = 0;
     sal_Int16 nOldWritingMode = 0;
-    xParagraph->getPropertyValue(u"ParaLastLineAdjust"_ustr) >>= eOldLastLine;
     xParagraph->getPropertyValue(u"ParaLeftMargin"_ustr) >>= nOldLeftMargin;
@@ -1236,4 +1234,3 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
             {"SetParagraphProperties.0": {
                 "Paragraph": 0,
-                "LastLineAlignment": "right",
                 "LeftMargin": 1200,
@@ -1272,7 +1269,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
         xParagraph = paragraphProperties();
-        style::ParagraphAdjust eLastLine = style::ParagraphAdjust_LEFT;
         sal_Int32 nValue = 0;
         sal_Int16 nWritingMode = 0;
-        xParagraph->getPropertyValue(u"ParaLastLineAdjust"_ustr) >>= eLastLine;
-        CPPUNIT_ASSERT_EQUAL(style::ParagraphAdjust_RIGHT, eLastLine);
         xParagraph->getPropertyValue(u"ParaLeftMargin"_ustr) >>= nValue;
@@ -1304,7 +1298,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
     xParagraph = paragraphProperties();
-    style::ParagraphAdjust eLastLine = style::ParagraphAdjust_LEFT;
     sal_Int32 nValue = 0;
     sal_Int16 nWritingMode = 0;
-    xParagraph->getPropertyValue(u"ParaLastLineAdjust"_ustr) >>= eLastLine;
-    CPPUNIT_ASSERT_EQUAL(eOldLastLine, eLastLine);
     xParagraph->getPropertyValue(u"ParaLeftMargin"_ustr) >>= nValue;
diff --git a/svx/inc/sdr/contact/viewcontactofsdrmediaobj.hxx b/svx/inc/sdr/contact/viewcontactofsdrmediaobj.hxx
--- a/svx/inc/sdr/contact/viewcontactofsdrmediaobj.hxx
+++ b/svx/inc/sdr/contact/viewcontactofsdrmediaobj.hxx
@@ -61,4 +61,1 @@ private:
-    // get notified if some properties have changed
-    void mediaPropertiesChanged(const ::avmedia::MediaItem& rNewState);
-
     // This method is responsible for creating the graphical visualisation data
diff --git a/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx b/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
--- a/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
+++ b/svx/source/sdr/contact/viewcontactofsdrmediaobj.cxx
@@ -94,6 +94,1 @@ void ViewContactOfSdrMediaObj::executeMediaItem( const ::avmedia::MediaItem& rItem )
-void ViewContactOfSdrMediaObj::mediaPropertiesChanged( const ::avmedia::MediaItem& rNewState )
-{
-    static_cast< SdrMediaObj& >(GetSdrObject()).mediaPropertiesChanged(rNewState);
-}
-
 void ViewContactOfSdrMediaObj::createViewIndependentPrimitive2DSequence(drawinglayer::primitive2d::Primitive2DDecompositionVisitor& rVisitor) const
diff --git a/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx b/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
--- a/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
+++ b/svx/source/sdr/contact/viewobjectcontactofsdrmediaobj.cxx
@@ -163,4 +163,4 @@ void ViewObjectContactOfSdrMediaObj::executeMediaItem( const ::avmedia::MediaItem& rItem )
-        // query new properties after trying to set the new properties
+        // Player feedback may contain transient defaults while media is unavailable.
+        // Keep it in the view; only setMediaProperties authors document properties.
         updateMediaItem( aUpdatedItem );
-        static_cast< ViewContactOfSdrMediaObj& >( GetViewContact() ).mediaPropertiesChanged( aUpdatedItem );
     }
diff --git a/svx/source/svdraw/svdpage.cxx b/svx/source/svdraw/svdpage.cxx
index 2d9e7a1d5..516709fa4 100644
--- a/svx/source/svdraw/svdpage.cxx
+++ b/svx/source/svdraw/svdpage.cxx
@@ -927,10 +927,10 @@ void SdrObjList::SetObjectNavigationPosition (
         return;

     mxNavigationOrder->erase(iObject);
-    sal_uInt32 nInsertPosition (nNewPosition);
-    // Adapt insertion position for the just erased object.
-    if (nNewPosition >= nOldPosition)
-        nInsertPosition -= 1;
+    // nNewPosition is the desired final index. Erasing the old entry has
+    // already shifted later entries left; subtracting once more would leave
+    // a forward move one position short (including a move from 0 to 1).
+    const sal_uInt32 nInsertPosition(nNewPosition);
     if (nInsertPosition >= mxNavigationOrder->size())
         mxNavigationOrder->push_back(aReference);
     else
