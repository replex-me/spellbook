diff --git a/oox/source/ppt/slidetransition.cxx b/oox/source/ppt/slidetransition.cxx
--- a/oox/source/ppt/slidetransition.cxx
+++ b/oox/source/ppt/slidetransition.cxx
@@ -82,7 +82,8 @@ void SlideTransition::setSlideProperties( PropertyMap & aProps )
                 aProps.setProperty( PROP_TransitionDuration, mfTransitionDurationInSeconds);
             aProps.setProperty( PROP_TransitionFadeColor, mnTransitionFadeColor);
             if( mnAdvanceTime != -1 ) {
-                aProps.setProperty( PROP_Duration, mnAdvanceTime/1000);
+                // OOXML advTm is milliseconds; Duration truncates to whole seconds.
+                aProps.setProperty( PROP_HighResDuration, mnAdvanceTime / 1000.0);
                 aProps.setProperty( PROP_Change, static_cast<sal_Int32>(1));
             }
         }
diff --git a/oox/source/ppt/slidetransitioncontext.cxx b/oox/source/ppt/slidetransitioncontext.cxx
--- a/oox/source/ppt/slidetransitioncontext.cxx
+++ b/oox/source/ppt/slidetransitioncontext.cxx
@@ -55,5 +55,8 @@ SlideTransitionContext::SlideTransitionContext( FragmentHandler2 const & rParent,
     // careful. if missing, no auto advance... 0 looks like a valid value
     // for auto advance
     if(rAttribs.hasAttribute( XML_advTm ))
+    {
         maTransition.setOoxAdvanceTime( rAttribs.getInteger( XML_advTm, -1 ) );
+        mbHasTransition = true; // Timing-only <p:transition> has no effect child.
+    }
 }
diff --git a/oox/source/token/properties.txt b/oox/source/token/properties.txt
--- a/oox/source/token/properties.txt
+++ b/oox/source/token/properties.txt
@@ -711,3 +711,4 @@
 XLA1Representation
 ZoomType
 ZoomValue
+HighResDuration
diff --git a/sd/source/filter/eppt/pptx-epptooxml.cxx b/sd/source/filter/eppt/pptx-epptooxml.cxx
--- a/sd/source/filter/eppt/pptx-epptooxml.cxx
+++ b/sd/source/filter/eppt/pptx-epptooxml.cxx
@@ -1600,15 +1600,21 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
         }
     }
 
-    // check if we resolved what transition to export or time is set
-    if (!nPPTTransitionType && !bOOXmlSpecificTransition && !isTransitionDurationSet)
-        return;
-
     if (ImplGetPropertyValue(mXPagePropSet, u"Change"_ustr))
         mAny >>= changeType;
 
-    // 1 means automatic, 2 half automatic - not sure what it means - at least I don't see it in UI
-    if (changeType == 1 && ImplGetPropertyValue(mXPagePropSet, u"Duration"_ustr))
-        mAny >>= advanceTiming;
+    // advTm is the slide's auto-advance time in milliseconds, independent of
+    // whether a visual transition effect is selected.
+    if (changeType == 1 && ImplGetPropertyValue(mXPagePropSet, u"HighResDuration"_ustr))
+    {
+        double fAdvanceSeconds = -1.0;
+        if ((mAny >>= fAdvanceSeconds) && fAdvanceSeconds >= 0.0
+            && fAdvanceSeconds <= 2147483.0)
+            advanceTiming = static_cast<sal_Int32>(std::round(fAdvanceSeconds * 1000.0));
+    }
+
+    if (!nPPTTransitionType && !bOOXmlSpecificTransition && !isTransitionDurationSet
+        && advanceTiming < 0)
+        return;
 
     if (!bOOXmlSpecificTransition)
@@ -1721,5 +1727,5 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
             pFS->startElementNS(XML_p, XML_transition,
                 XML_spd, speed,
-                XML_advTm, OString::number(advanceTiming * 1000),
+                XML_advTm, OString::number(advanceTiming),
                 FSNS(XML_p14, XML_dur), OString::number(nTransitionDuration));
         }
@@ -1734,4 +1740,4 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
             pFS->startElementNS(XML_p, XML_transition,
                 XML_spd, speed,
-                XML_advTm, OString::number(advanceTiming * 1000));
+                XML_advTm, OString::number(advanceTiming));
         }
@@ -1773,5 +1779,5 @@ void PowerPointExport::WriteTransition(const FSHelperPtr& pFS)
     pFS->startElementNS(XML_p, XML_transition,
         XML_spd, speed,
-        XML_advTm, sax_fastparser::UseIf(OString::number(advanceTiming * 1000), isAdvanceTimingSet));
+        XML_advTm, sax_fastparser::UseIf(OString::number(advanceTiming), isAdvanceTimingSet));
 
     if (nTransition)
diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -1396,13 +1396,15 @@ struct SlideMetadataState
 {
     sd::HeaderFooterSettings maHeaderFooter;
     double mfHighResolutionDuration = 0.0;
+    PresChange mePresChange = PresChange::Manual;
     bool mbBackgroundObjectsVisible = false;
 
     bool operator==(const SlideMetadataState& rOther) const
     {
         return maHeaderFooter == rOther.maHeaderFooter
                && rtl::math::approxEqual(mfHighResolutionDuration,
                                          rOther.mfHighResolutionDuration)
+               && mePresChange == rOther.mePresChange
                && mbBackgroundObjectsVisible == rOther.mbBackgroundObjectsVisible;
     }
 };
@@ -1415,6 +1417,7 @@ SlideMetadataState lcl_GetSlideMetadata(SdDrawDocument& rDoc, SdPage& rPage)
     return {
         rPage.getHeaderFooterSettings(),
         rPage.GetTime(),
+        rPage.GetPresChange(),
         aVisibleLayers.IsSet(aBackgroundObjects),
     };
 }
@@ -1425,4 +1428,5 @@ void lcl_SetSlideMetadata(SdDrawDocument& rDoc, SdPage& rPage,
 {
     rPage.setHeaderFooterSettings(rState.maHeaderFooter);
     rPage.SetTime(rState.mfHighResolutionDuration);
+    rPage.SetPresChange(rState.mePresChange);
     SdrLayerAdmin& rLayerAdmin = rDoc.GetLayerAdmin();
@@ -3127,5 +3131,5 @@ void handleSetSlideProperties(SlideCommandContext& rCtx, const std::string& /*rKey*/,
     static const std::set<std::string> aAllowedKeys = {
         "IsFooterVisible", "FooterText", "IsPageNumberVisible",
         "IsDateTimeVisible", "IsDateTimeFixed", "DateTimeText",
-        "DateTimeFormat", "HighResDuration", "IsBackgroundObjectsVisible",
+        "DateTimeFormat", "HighResDuration", "AutoAdvance", "IsBackgroundObjectsVisible",
     };
@@ -3182,7 +3186,16 @@ void handleSetSlideProperties(SlideCommandContext& rCtx, const std::string& /*rKey*/,
         if (!std::isfinite(*oDuration) || *oDuration < 0.0 || *oDuration > 86400.0)
             throw lang::IllegalArgumentException(u"Slide duration is invalid"_ustr, nullptr, 0);
         aNewState.mfHighResolutionDuration = *oDuration;
+        aNewState.mePresChange = PresChange::Auto;
         bSupplied = true;
     }
+    if (const auto oAutoAdvance = rValue.get_optional<bool>("AutoAdvance"))
+    {
+        if (!*oAutoAdvance && rValue.get_optional<double>("HighResDuration"))
+            throw lang::IllegalArgumentException(
+                u"Manual slide advance cannot carry a duration"_ustr, nullptr, 0);
+        aNewState.mePresChange = *oAutoAdvance ? PresChange::Auto : PresChange::Manual;
+        bSupplied = true;
+    }
     if (!bSupplied)
         throw lang::IllegalArgumentException(u"No slide properties supplied"_ustr, nullptr, 0);
diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -1178,3 +1178,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
     const sd::HeaderFooterSettings aOldHeaderFooter = pPage->getHeaderFooterSettings();
     const double fOldTime = pPage->GetTime();
+    const PresChange eOldPresChange = pPage->GetPresChange();
     SdrLayerAdmin& rLayerAdmin = pDoc->GetLayerAdmin();
@@ -1262,3 +1263,4 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
         CPPUNIT_ASSERT_EQUAL(u"2026-09-16"_ustr, rSettings.maDateTimeText);
         CPPUNIT_ASSERT_DOUBLES_EQUAL(12.5, pPage->GetTime(), 0.001);
+        CPPUNIT_ASSERT(pPage->GetPresChange() == PresChange::Auto);
         CPPUNIT_ASSERT(!pPage->TRG_GetMasterPageVisibleLayers().IsSet(aBackgroundObjects));
@@ -1294,4 +1296,5 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMetadataLineAndParagraphUndoRedo)
     pUndoManager->Undo();
     CPPUNIT_ASSERT(aOldHeaderFooter == pPage->getHeaderFooterSettings());
     CPPUNIT_ASSERT_DOUBLES_EQUAL(fOldTime, pPage->GetTime(), 0.001);
+    CPPUNIT_ASSERT(pPage->GetPresChange() == eOldPresChange);
     CPPUNIT_ASSERT_EQUAL(
