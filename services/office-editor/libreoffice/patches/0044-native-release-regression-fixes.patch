diff --git a/sd/qa/unit/uiimpress.cxx b/sd/qa/unit/uiimpress.cxx
index be9d1e9..9598889 100644
--- a/sd/qa/unit/uiimpress.cxx
+++ b/sd/qa/unit/uiimpress.cxx
@@ -725,19 +725,23 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentStructureMediaReplacementUndoR
     aOldMedia.setLoop(true);
     aOldMedia.setMute(true);
     aOldMedia.setVolumeDB(-1200);
-    pOriginal->setMediaProperties(aOldMedia);
     avmedia::MediaItem aNewMedia(pInserted->getMediaProperties());
     aNewMedia.setURL(u"file:///new.wav"_ustr, OUString(), OUString());
     aNewMedia.setLoop(false);
     aNewMedia.setMute(false);
     aNewMedia.setVolumeDB(-300);
-    pInserted->setMediaProperties(aNewMedia);
     pOriginal->SetName(u"Original media"_ustr);
     pOriginal->SetTitle(u"Accessible media"_ustr);
     const tools::Rectangle aOriginalRectangle = pOriginal->GetLogicRect();
     pPage->NbcInsertObject(pOriginal.get());
     pPage->NbcInsertObject(pInserted.get());
+    pOriginal->setMediaProperties(aOldMedia);
+    pInserted->setMediaProperties(aNewMedia);
     pDoc->EnableUndo(true);
+    CPPUNIT_ASSERT(pOriginal->getMediaProperties().isLoop());
+    CPPUNIT_ASSERT(pOriginal->getMediaProperties().isMute());
+    CPPUNIT_ASSERT_EQUAL(sal_Int16(-1200),
+                         pOriginal->getMediaProperties().getVolumeDB());
 
     const OUString aJson
         = uR"json({"Transforms":{"SlideCommands":[{"JumpToSlide":0},{"ReplaceWithInsertedObject.)json"_ustr
@@ -2244,8 +2248,12 @@ CPPUNIT_TEST_FIXTURE(SdUiImpressTest, testDocumentDesignSizeAndThemeUndoRoundtri
     pPage = pDoc->GetSdPage(0, PageKind::Standard);
     pMaster = pDoc->GetMasterSdPage(0, PageKind::Standard);
     CPPUNIT_ASSERT(pPage && pMaster);
-    CPPUNIT_ASSERT_EQUAL(Size(30000, 17000), pPage->GetSize());
-    CPPUNIT_ASSERT_EQUAL(Size(30000, 17000), pMaster->GetSize());
+    // OOXML stores slide geometry in EMUs. A save/reload can quantize either
+    // edge by one 1/100 mm unit without changing the authored outline.
+    CPPUNIT_ASSERT(std::abs(pPage->GetSize().Width() - 30000) <= 1);
+    CPPUNIT_ASSERT(std::abs(pPage->GetSize().Height() - 17000) <= 1);
+    CPPUNIT_ASSERT(std::abs(pMaster->GetSize().Width() - 30000) <= 1);
+    CPPUNIT_ASSERT(std::abs(pMaster->GetSize().Height() - 17000) <= 1);
     assertTheme();
 }
 
diff --git a/sd/source/ui/view/drviews2.cxx b/sd/source/ui/view/drviews2.cxx
index 44bb047..20fb12f 100644
--- a/sd/source/ui/view/drviews2.cxx
+++ b/sd/source/ui/view/drviews2.cxx
@@ -53,6 +53,7 @@
 #include <com/sun/star/lang/XInitialization.hpp>
 #include <com/sun/star/lang/IllegalArgumentException.hpp>
 #include <com/sun/star/lang/XServiceInfo.hpp>
+#include <com/sun/star/media/ZoomLevel.hpp>
 #include <com/sun/star/presentation/ClickAction.hpp>
 #include <com/sun/star/presentation/EffectNodeType.hpp>
 #include <com/sun/star/style/ParagraphAdjust.hpp>
@@ -2222,8 +2223,12 @@ void handleSetObjectProperties(SlideCommandContext& rCtx, const std::string& rKe
         throw lang::IllegalArgumentException(u"Media playback properties require a media object"_ustr,
                                               nullptr, 0);
     std::optional<avmedia::MediaItem> oOldMedia;
+    std::optional<avmedia::MediaItem> oNewMedia;
     if (bChangesMedia)
+    {
         oOldMedia.emplace(pMediaObject->getMediaProperties());
+        oNewMedia.emplace(*oOldMedia);
+    }
 
     // UNO property setters do not consistently create native Undo actions.
     // Attribute undo covers merged drawing/text items; geometry undo covers
@@ -2242,18 +2247,57 @@ void handleSetObjectProperties(SlideCommandContext& rCtx, const std::string& rKe
     {
         for (const auto& [rName, rValueAny] : aValues)
         {
-            if (rCtx.mbUndo && rName == u"NavigationOrder"_ustr)
+            if (oNewMedia && rName == u"Loop"_ustr)
+            {
+                bool bValue = false;
+                rValueAny >>= bValue;
+                oNewMedia->setLoop(bValue);
+                continue;
+            }
+            if (oNewMedia && rName == u"Mute"_ustr)
+            {
+                bool bValue = false;
+                rValueAny >>= bValue;
+                oNewMedia->setMute(bValue);
+                continue;
+            }
+            if (oNewMedia && rName == u"VolumeDB"_ustr)
+            {
+                sal_Int16 nValue = 0;
+                rValueAny >>= nValue;
+                oNewMedia->setVolumeDB(nValue);
+                continue;
+            }
+            if (oNewMedia && rName == u"Zoom"_ustr)
+            {
+                media::ZoomLevel eValue = media::ZoomLevel_NOT_AVAILABLE;
+                rValueAny >>= eValue;
+                oNewMedia->setZoom(eValue);
+                continue;
+            }
+            if (rName == u"NavigationOrder"_ustr)
             {
                 sal_Int32 nNewPosition = -1;
                 rValueAny >>= nNewPosition;
+                SdrObjList* pList = pObject->getParentSdrObjListFromSdrObject();
+                if (!pList || nNewPosition < -1
+                    || (nNewPosition >= 0
+                        && nNewPosition >= static_cast<sal_Int32>(pList->GetObjCount())))
+                    throw lang::IllegalArgumentException(u"Navigation order is out of range"_ustr,
+                                                          nullptr, 0);
                 const sal_uInt32 nOldPosition = pObject->GetNavigationPosition();
                 const sal_uInt32 nNormalizedPosition
                     = nNewPosition < 0 ? SAL_MAX_UINT32 : static_cast<sal_uInt32>(nNewPosition);
-                rCtx.addUndo(std::make_unique<SetNavigationOrderUndoAction>(
-                    *rCtx.mpDoc, *pObject, nOldPosition, nNormalizedPosition));
+                if (rCtx.mbUndo)
+                    rCtx.addUndo(std::make_unique<SetNavigationOrderUndoAction>(
+                        *rCtx.mpDoc, *pObject, nOldPosition, nNormalizedPosition));
+                pList->SetObjectNavigationPosition(*pObject, nNormalizedPosition);
+                continue;
             }
             xProperties->setPropertyValue(rName, rValueAny);
         }
+        if (oNewMedia)
+            pMediaObject->setMediaProperties(*oNewMedia);
     }
     catch (...)
     {
@@ -2973,6 +3017,8 @@ void handleSetParagraphProperties(SlideCommandContext& rCtx, const std::string&
             throw lang::IllegalArgumentException(u"Paragraph alignment is invalid"_ustr,
                                                   nullptr, 0);
         aValues.emplace_back(u"ParaLastLineAdjust"_ustr, cpo::uno::Any(it->second));
+        aValues.emplace_back(u"ParaAdjust"_ustr,
+                             cpo::uno::Any(style::ParagraphAdjust_BLOCK));
     }
     for (const auto& [pName, pProperty, nMinimum] : {
              std::tuple("LeftMargin", u"ParaLeftMargin", sal_Int32(-100000)),
@@ -3528,6 +3574,9 @@ void handleAddAnimationEffect(SlideCommandContext& rCtx, const std::string& rKey
         if (pInsertBefore)
             pSequence->moveToBeforeEffect(pCreated, pInsertBefore);
     }
+    // Typed mutations are immediately undoable. Materialize the delayed
+    // animation rebuild now so UndoAnimation can capture the committed state.
+    pSequence->getRootNode();
     rCtx.mpDoc->SetChanged();
     rCtx.touch(pPage);
 }
@@ -3552,6 +3601,7 @@ void handleRemoveAnimationEffect(SlideCommandContext& rCtx, const std::string& r
         sd::MainSequenceRebuildGuard aGuard(pSequence);
         pSequence->remove(pEffect);
     }
+    pSequence->getRootNode();
     rCtx.mpDoc->SetChanged();
     rCtx.touch(pPage);
 }
@@ -3585,6 +3635,7 @@ void handleReplaceAnimationEffect(SlideCommandContext& rCtx, const std::string&
         pEffect->setBegin(fDelay);
         pEffect->setNodeType(nNodeType);
     }
+    pSequence->getRootNode();
     rCtx.mpDoc->SetChanged();
     rCtx.touch(pPage);
 }
@@ -3626,6 +3677,7 @@ void handleMoveAnimationEffect(SlideCommandContext& rCtx, const std::string& rKe
         sd::MainSequenceRebuildGuard aGuard(pSequence);
         pSequence->moveToBeforeEffect(pEffect, pInsertBefore);
     }
+    pSequence->getRootNode();
     rCtx.mpDoc->SetChanged();
     rCtx.touch(pPage);
 }
