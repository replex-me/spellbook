diff --git a/oox/source/token/properties.txt b/oox/source/token/properties.txt
--- a/oox/source/token/properties.txt	2026-08-21 21:51:44
+++ b/oox/source/token/properties.txt	2026-09-02 15:28:47
@@ -57,6 +57,7 @@
 ChartExDimType
 ChartExFormula
 ChartExNFormula
+CharAutoKerning
 CharBackColor
 CharBackgroundComplexColor
 CharCaseMap
@@ -92,6 +93,7 @@
 CharPosture
 CharPostureAsian
 CharPostureComplex
+CharScriptHint
 CharShadowed
 CharStrikeout
 CharStyleName
@@ -431,6 +433,7 @@
 ParaBottomMargin
 ParaFirstLineIndent
 ParaIndent
+ParaIsForbiddenRules
 ParaIsHangingPunctuation
 ParaIsHyphenation
 ParaIsNumberingRestart
diff --git a/oox/inc/drawingml/textcharacterproperties.hxx b/oox/inc/drawingml/textcharacterproperties.hxx
--- a/oox/inc/drawingml/textcharacterproperties.hxx	2026-08-21 21:51:44
+++ b/oox/inc/drawingml/textcharacterproperties.hxx	2026-09-02 15:28:47
@@ -51,6 +51,8 @@
     /// If a font scale has to be applied manually to moHeight.
     std::optional< double > moFontScale;
     std::optional< sal_Int32 > moSpacing;
+    /// DrawingML kern: pair kerning threshold in hundredths of a point.
+    std::optional< sal_Int32 > moKerning;
     std::optional< sal_Int32 > moUnderline;
     std::optional< sal_Int32 > moBaseline;
     std::optional< sal_Int32 > moStrikeout;
diff --git a/oox/source/drawingml/textcharacterpropertiescontext.cxx b/oox/source/drawingml/textcharacterpropertiescontext.cxx
--- a/oox/source/drawingml/textcharacterpropertiescontext.cxx	2026-08-21 21:51:44
+++ b/oox/source/drawingml/textcharacterpropertiescontext.cxx	2026-09-02 15:28:47
@@ -65,6 +65,8 @@
         mrTextCharacterProperties.moHeight = rAttribs.getInteger( XML_sz );
     if ( rAttribs.hasAttribute( XML_spc ) )
         mrTextCharacterProperties.moSpacing = rAttribs.getInteger( XML_spc );
+    if ( rAttribs.hasAttribute( XML_kern ) )
+        mrTextCharacterProperties.moKerning = rAttribs.getInteger( XML_kern );
     if ( rAttribs.hasAttribute( XML_u ) )
         mrTextCharacterProperties.moUnderline = rAttribs.getToken( XML_u );
     if ( rAttribs.hasAttribute( XML_strike ) )
diff --git a/oox/source/drawingml/textcharacterproperties.cxx b/oox/source/drawingml/textcharacterproperties.cxx
--- a/oox/source/drawingml/textcharacterproperties.cxx	2026-08-21 21:51:44
+++ b/oox/source/drawingml/textcharacterproperties.cxx	2026-09-02 15:29:16
@@ -60,6 +61,7 @@
     assignIfUsed( moHeight, rSourceProps.moHeight );
     assignIfUsed( moFontScale, rSourceProps.moFontScale);
     assignIfUsed( moSpacing, rSourceProps.moSpacing );
+    assignIfUsed( moKerning, rSourceProps.moKerning );
     assignIfUsed( moUnderline, rSourceProps.moUnderline );
     assignIfUsed( moBaseline, rSourceProps.moBaseline );
     assignIfUsed( moStrikeout, rSourceProps.moStrikeout );
@@ -189,6 +187,10 @@

     rPropMap.setProperty( PROP_CharKerning, static_cast<sal_Int16>(GetTextSpacingPoint( moSpacing.value_or( 0 ) )));

+    // DrawingML kern: pair kerning applies when the run size reaches the threshold.
+    if( moKerning.has_value() && moHeight.has_value() )
+        rPropMap.setProperty( PROP_CharAutoKerning, moKerning.value() > 0 && moHeight.value() >= moKerning.value() );
+
     rPropMap.setProperty( PROP_CharUnderline, GetFontUnderline( moUnderline.value_or( XML_none ) ));
     rPropMap.setProperty( PROP_CharStrikeout, GetFontStrikeout( moStrikeout.value_or( XML_noStrike ) ));
     rPropMap.setProperty( PROP_CharCaseMap, GetCaseMap( moCaseMap.value_or( XML_none ) ));
diff --git a/oox/source/drawingml/textparagraphpropertiescontext.cxx b/oox/source/drawingml/textparagraphpropertiescontext.cxx
--- a/oox/source/drawingml/textparagraphpropertiescontext.cxx	2026-08-21 21:51:44
+++ b/oox/source/drawingml/textparagraphpropertiescontext.cxx	2026-09-02 15:28:47
@@ -105,7 +105,12 @@
         }
     }

-//  bool bEaLineBrk = rAttribs.getBool( XML_eaLnBrk, true );
+    // eaLnBrk: whether the East Asian line breaking rules (kinsoku) apply.
+    if ( rAttribs.hasAttribute( XML_eaLnBrk ) )
+    {
+        bool bEaLineBrk = rAttribs.getBool( XML_eaLnBrk, true );
+        rPropertyMap.setProperty( PROP_ParaIsForbiddenRules, bEaLineBrk );
+    }
     if ( rAttribs.hasAttribute( XML_latinLnBrk ) )
     {
         bool bLatinLineBrk = rAttribs.getBool( XML_latinLnBrk, true );
diff --git a/oox/source/drawingml/textbodypropertiescontext.cxx b/oox/source/drawingml/textbodypropertiescontext.cxx
--- a/oox/source/drawingml/textbodypropertiescontext.cxx	2026-08-21 21:51:44
+++ b/oox/source/drawingml/textbodypropertiescontext.cxx	2026-09-02 15:28:47
@@ -239,8 +239,9 @@
                 mrTextBodyProp.maPropertyMap.setProperty(PROP_TextAutoGrowHeight, false);
                 mrTextBodyProp.mnFontScale = rAttribs.getInteger(XML_fontScale, 100000);
                 mrTextBodyProp.mnSpacingScale = rAttribs.getInteger(XML_lnSpcReduction, 100000);
-                mrTextBodyProp.maPropertyMap.setProperty(PROP_TextFitToSizeFontScale, mrTextBodyProp.mnFontScale / 100000.0);
-                mrTextBodyProp.maPropertyMap.setProperty(PROP_TextFitToSizeSpacingScale, 1.0 - mrTextBodyProp.mnSpacingScale / 100000.0);
+                // The shape properties take percentages (SvxShape divides by 100).
+                mrTextBodyProp.maPropertyMap.setProperty(PROP_TextFitToSizeFontScale, mrTextBodyProp.mnFontScale / 1000.0);
+                mrTextBodyProp.maPropertyMap.setProperty(PROP_TextFitToSizeSpacingScale, (100000 - mrTextBodyProp.mnSpacingScale) / 1000.0);
                 break;
             }
             case A_TOKEN( spAutoFit ):
diff --git a/oox/source/drawingml/textrun.cxx b/oox/source/drawingml/textrun.cxx
--- a/oox/source/drawingml/textrun.cxx
+++ b/oox/source/drawingml/textrun.cxx
@@ -26,7 +26,12 @@
 #include <com/sun/star/beans/XPropertyState.hpp>
 #include <com/sun/star/lang/XMultiServiceFactory.hpp>
 #include <com/sun/star/text/XTextField.hpp>
+#include <com/sun/star/text/ScriptHintType.hpp>
+#include <com/sun/star/i18n/ScriptType.hpp>
+#include <i18nlangtag/languagetag.hxx>
+#include <i18nlangtag/mslangid.hxx>
+#include <i18nutil/unicode.hxx>

 #include <sal/log.hxx>

 #include <oox/helper/helper.hxx>
@@ -84,7 +88,41 @@
             // UNO API has the character height as float, DML has it as int, but in hundreds.
             aTextCharacterProps.moHeight = static_cast<sal_Int32>(nDefaultCharHeight * 100);
+        // Transport the declared language to scripts actually present in this
+        // run. Common/inherited characters do not overwrite unused locale
+        // slots: doing so changes contextual line breaking across punctuation.
+        sal_uInt8 nLanguageScripts = 0;
+        if (aTextCharacterProps.moLang.has_value())
+        {
+            for (sal_Int32 nOffset = 0; nOffset < getText().getLength();)
+            {
+                const sal_uInt32 nCharacter = getText().iterateCodePoints(&nOffset);
+                UErrorCode nError = U_ZERO_ERROR;
+                const auto eScript = uscript_getScript(nCharacter, &nError);
+                if (U_FAILURE(nError))
+                    continue;
+                switch (unicode::getScriptClassFromUScriptCode(eScript))
+                {
+                    case css::i18n::ScriptType::LATIN: nLanguageScripts |= 1; break;
+                    case css::i18n::ScriptType::ASIAN: nLanguageScripts |= 2; break;
+                    case css::i18n::ScriptType::COMPLEX: nLanguageScripts |= 4; break;
+                }
+            }
+        }
+        const auto applyRunLanguage = [&](PropertySet& rProperties) {
+            if (!nLanguageScripts)
+                return;
+            const auto aLocale = LanguageTag(*aTextCharacterProps.moLang).getLocale();
+            if (nLanguageScripts & 1)
+                rProperties.setProperty(PROP_CharLocale, aLocale);
+            if (nLanguageScripts & 2)
+                rProperties.setProperty(PROP_CharLocaleAsian, aLocale);
+            if (nLanguageScripts & 4)
+                rProperties.setProperty(PROP_CharLocaleComplex, aLocale);
+        };
         aTextCharacterProps.pushToPropSet( aPropSet, rFilterBase );
+        applyRunLanguage(aPropSet);
+        aPropSet.setProperty(PROP_CharScriptHint, css::text::ScriptHintType::AUTOMATIC);

         if( maTextCharacterProperties.maHyperlinkPropertyMap.empty() )
         {
             if( mbIsLineBreak )
@@ -96,13 +101,24 @@
             {
                 sal_Int32 nIndex = 0;
                 sal_Int32 nMax = getText().getLength();
+                // DrawingML's Latin slot supplies ASCII in non-complex runs.
+                // Never apply this hint to stars, circled digits, CJK punctuation,
+                // symbols or complex-script text: they retain contextual selection.
+                const bool bComplex = aTextCharacterProps.moLang.has_value()
+                    && MsLangId::getScriptType(LanguageTag(*aTextCharacterProps.moLang).getLanguageType())
+                        == css::i18n::ScriptType::COMPLEX;
+                const auto useLatinSlot = [bComplex](sal_Unicode c) {
+                    return !bComplex && c >= 0x20 && c <= 0x7e;
+                };
                 while(true)
                 {
                     bool bSymbol = (getText()[nIndex] & 0xff00) == 0xf000;
+                    const bool bLatinSlot = useLatinSlot(getText()[nIndex]);
                     sal_Int32 nCount = 1;
                     while(nIndex + nCount < nMax
-                            && ((getText()[nIndex + nCount] & 0xff00) == 0xf000) == bSymbol)
+                            && ((getText()[nIndex + nCount] & 0xff00) == 0xf000) == bSymbol
+                            && useLatinSlot(getText()[nIndex + nCount]) == bLatinSlot)
                         ++nCount;

                     OUString aFontName;
                     sal_Int16 nFontFamily = 0, nFontPitch = 0;
@@ -125,8 +141,10 @@
                         bReset = true;
                     }

                     OUString aSubString(getText().copy(nIndex, nCount));
+                    aPropSet.setProperty(PROP_CharScriptHint, (bLatinSlot || bReset)
+                        ? css::text::ScriptHintType::LATIN : css::text::ScriptHintType::AUTOMATIC);
                     xText->insertString(xStart, aSubString, false);

                     aPropSet = PropertySet(xStart);
                     // Reset to whatever it was.
@@ -141,7 +159,9 @@
                         break;

                     aTextCharacterProps.pushToPropSet(aPropSet, rFilterBase);
+                    applyRunLanguage(aPropSet);
                 }
+                aPropSet.setProperty(PROP_CharScriptHint, css::text::ScriptHintType::AUTOMATIC);
             }
         }
         else
@@ -177,5 +230,6 @@
                 PropertySet aFieldTextPropSet( xTextFieldCursor );
                 aTextCharacterProps.pushToPropSet( aFieldTextPropSet, rFilterBase );
+                applyRunLanguage(aFieldTextPropSet);
             }
             else
             {
diff --git a/oox/Library_oox.mk b/oox/Library_oox.mk
--- a/oox/Library_oox.mk
+++ b/oox/Library_oox.mk
@@ -47,6 +47,7 @@
     drawinglayer \
     editeng \
     i18nlangtag \
+    i18nutil \
     msfilter \
     sal \
     sax \
@@ -67,4 +68,6 @@
 $(eval $(call gb_Library_use_externals,oox,\
 	boost_headers \
 	frozen \
+	icu_headers \
+	icuuc \
 ))
