diff --git a/vcl/unx/generic/fontmanager/fontconfig.cxx b/vcl/unx/generic/fontmanager/fontconfig.cxx
--- a/vcl/unx/generic/fontmanager/fontconfig.cxx
+++ b/vcl/unx/generic/fontmanager/fontconfig.cxx
@@ -953,6 +953,13 @@
                     sLang != "ctg" &&
                     sLang != "sa";
                 break;
+            // A run may be tagged with a Latin editing language while its
+            // missing glyphs are Hangul. Select fallback fonts for that script
+            // without changing the run's language or its line-breaking rules.
+            // Korean and Jeju use Hangul; explicit script tags are handled above.
+            case USCRIPT_HANGUL:
+                bIsImpossible = sLang != "ko" && sLang != "jje";
+                break;
             default:
                 break;
         }
